# 로스제로 화면 명세 — 최초 설정

## 1. 문서 목적

`로스제로_화면_명세.md`가 다루지 않는 **최초 1회 구간**입니다. 서비스 플로우의 `앱 열기 → 품목 등록 완료? → 아니오 → 등록` 분기에 해당하며, 근거는 `로스제로_MVP_기획서.md`의 F-01·F-03·F-13과 3-1장(A등급 안의 두 종류)입니다.

화면 ID는 매일 반복하는 루프(LZ-01~LZ-14)와 구분해 `LZ-S` 계열을 씁니다.

## 2. 여기서 지킬 원칙

**시작 전에 다 등록시키지 않습니다.** 재고관리 앱을 그만두는 가장 흔한 이유가 초기 등록 부담입니다. 그래서 최초 설정에서 받는 것과, 쓰면서 채우는 것을 나눴습니다.

| 최초에 받는 것 | 쓰면서 채우는 것 |
|---|---|
| 매장 정보 (영업일·마감 시각) | **메뉴와 판매가** — 첫 사진에서 잡힌 메뉴에 물어봅니다 |
| 품목 (거래처·납품요일·매입단가 등) | **메뉴 레시피** — 많이 팔린 메뉴부터 하루 1~2개씩 (LZ-03) |
| 직접 만드는 품목의 제조 레시피 | |
| 매장 설정 (고정비·인건비·과세·카드) | |

**메뉴를 미리 등록시키지 않는 이유**는 포스 사진에서 자동으로 잡히기 때문입니다. 사장이 메뉴 30개를 손으로 치는 일을 만들지 않습니다.

## 3. 핵심 흐름

`로그인 → 매장 정보 → 품목 등록 → (직접 만드는 품목이면) 제조 레시피 → 매장 설정 → 설정 완료 → 오늘 할 일 홈(LZ-01)`

품목 등록은 1회로 끝나지 않습니다. 메뉴가 바뀌거나 거래처가 바뀌면 다시 들어옵니다. **LZ-S3은 홈에서 언제든 진입할 수 있어야 합니다.**

## 4. 화면 목록

| 화면 ID | 화면 이름 | 목적 | 핵심 행동 | 다음 화면 |
|---|---|---|---|---|
| LZ-S1 | 로그인 | 매장 계정으로 접속한다 | 로그인 | LZ-S2 / LZ-01 |
| LZ-S2 | 매장 정보 | 입력 알림과 발주 요일 판정의 기준값을 정한다 | 영업일·마감 시각 선택 | LZ-S3 |
| LZ-S3 | 품목 목록 | 등록된 품목을 확인하고 관리한다 | 품목 추가, 품목 선택 | LZ-S4 / LZ-S6 |
| LZ-S4 | 품목 등록·수정 | 품목 하나의 조건을 저장한다 | 구분 선택, 값 입력, 저장 | LZ-S5 / LZ-S3 |
| LZ-S5 | 제조 레시피 | 직접 만드는 품목의 한 판 구성을 저장한다 | 재료·산출량·시간 입력 | LZ-S3 |
| LZ-S6 | 매장 설정 | 수익률 계산에 쓸 값을 받는다 | 고정비·인건비·과세·카드 입력 | LZ-S7 |
| LZ-S7 | 설정 완료 | 오늘부터 무엇을 하면 되는지 안내한다 | 오늘 시작 | LZ-01 |

`LZ-S6`은 매일 루프의 `LZ-13 매장 설정`과 **같은 화면**입니다. 최초 흐름에서 한 번 지나가고, 이후에는 결과 확인에서 들어옵니다.

---

## 5. 화면별 상세 명세

### LZ-S1 로그인

- 목적: 매장 계정으로 접속한다.
- 입력값: 이메일, 비밀번호
- 버튼: `로그인`
- 기본 상태: 두 값이 채워지기 전까지 버튼 비활성
- 오류 상태: 인증 실패 시 어느 값이 틀렸는지 특정하지 않고 한 문장으로 안내
- 다음 화면: 매장 정보가 없으면 LZ-S2, 품목이 0개면 LZ-S3, 둘 다 있으면 LZ-01
- 확인 필요: 파일럿 3곳은 계정을 직접 만들어 전달할 수 있습니다. 앱 안에서 가입을 받을지 초대만으로 운영할지는 W7 파일럿 시작 전에 정합니다

### LZ-S2 매장 정보

- 목적: 입력 알림과 "오늘이 납품 요일인가" 판정의 기준값을 받는다.
- 입력값: 매장 이름, 영업일(요일 다중 선택), 마감 시각
- 버튼: `다음`
- 기본 상태: 영업일은 월~일 전체가 선택된 상태로 시작하고 쉬는 요일만 해제한다
- 안내: 마감 시각은 입력 알림 기준이자 품절 시각의 기본값이라는 점을 한 문장으로 표시
- 오류 상태: 영업일이 하나도 없으면 저장 불가
- 다음 화면: LZ-S3
- 주의: **매장 이름·주소·연락처는 AI 요청에 담지 않습니다.** 알림과 화면 표시에만 씁니다
- 영업일수는 고정비를 하루치로 나눌 때도 쓰입니다. 한 달 영업일수가 여기서 계산됩니다

### LZ-S3 품목 목록

- 목적: 관리 중인 품목을 확인하고 추가·수정한다.
- 표시값: 품목명, 구분 배지, 거래처, 납품 요일, 매입 단가, 등록 품목 수
- 버튼: `품목 추가`, 품목 행, `다음`
- 기본 상태: **구분별로 묶어서** 표시한다 — 사 옴 / 직접 만듦 / 원부자재
- 빈 상태: 품목이 0개면 무엇을 등록해야 하는지 예시와 함께 설명하고 `품목 추가`만 표시. `다음` 비활성
- 안내 상태: 등록 품목이 20종을 넘으면 매일 폐기 입력 화면이 길어진다는 안내를 표시한다. 등록을 막지는 않는다
- 오류 상태: 목록을 불러오지 못하면 재시도 버튼
- 다음 화면: 추가·선택 시 LZ-S4, 다음 선택 시 LZ-S6(최초) 또는 LZ-01(수정 진입인 경우)

### LZ-S4 품목 등록·수정

- 목적: 품목 하나의 조건을 저장한다. **여기서 빠진 값은 추천도 원가도 계산할 수 없습니다.**

#### 먼저 구분을 고른다

| 구분 | 예 | 원가가 어디서 나오나 |
|---|---|---|
| **사 옴** | 크로플, 초코쿠키 | 매입 단가가 곧 원가 |
| **직접 만듦** | 바스크치즈케이크 | 재료값 ÷ 나오는 개수 (LZ-S5에서 받는다) |
| **원부자재** | 우유, 생크림, 크림치즈, 원두 | 매입 단가 |

구분에 따라 아래 입력란이 달라집니다. 구분을 먼저 묻는 이유는, 직접 만드는 품목에 매입 단가를 물어보면 사장이 무엇을 넣어야 할지 몰라 멈추기 때문입니다.

#### 입력값

| 값 | 사 옴 | 직접 만듦 | 원부자재 | 쓰이는 곳 |
|---|:-:|:-:|:-:|---|
| 품목명 | 필수 | 필수 | 필수 | 전 화면 |
| 매입 단가 | 필수 | — | 필수 | 원가·폐기 금액 |
| 용량과 단위 (1L, 500ml, 1kg) | — | — | 필수 | 레시피 환산 |
| 한 판 산출 개수 | — | 필수 | — | 제조량 추천 |
| 유통기한 일수 | 필수 | 필수 | 필수 | 임박 판정·소진 제안 |
| 거래처 | 필수 | — | 필수 | 발주 내역 묶음 |
| 납품 요일 | 필수 | — | 필수 | 발주 요일 판정 |
| 주문 마감 시각 | 필수 | — | 필수 | 확정 완료 안내 |
| 리드타임 | 필수 | — | 필수 | 필요량 계산 |
| 최소 주문 단위 | 필수 | — | 필수 | 제안 수량 반올림 |
| **면세 품목인가** | 선택 | — | 선택 | 부가세 매입세액 공제 |
| **발주 추천 대상** | 기본 켬 | — | 기본 켬 | 발주 목록에 넣을지 |

- 버튼: `저장`, `삭제`(수정 진입 시)
- 기본 상태: 필수값이 하나라도 비면 저장 비활성
- 입력 보조: 거래처·납품 요일·주문 마감 시각은 이전에 등록한 거래처를 고르면 자동으로 채운다. **같은 값을 품목마다 반복 입력하게 두지 않는다**
- 단위 안내: 최소 주문 단위는 낱개인지 박스인지 고르고, 박스면 한 박스에 몇 개인지 받는다
- 면세 안내: 우유·계란·과일 등 가공하지 않은 농축수산물은 면세라 **매입세액 공제가 안 됩니다.** 체크하면 부가세가 그만큼 더 나옵니다
- 발주 추천 대상: 원두처럼 유통기한이 긴 재료는 원가 계산에는 반드시 들어가되 발주 추천은 끌 수 있습니다. **원가 계산과 발주 관리는 별개입니다**
- 경고 상태: 삭제 시 지난 기록도 함께 사라진다는 점을 확인받는다
- 오류 상태: 같은 이름의 품목이 있으면 저장 전에 알린다
- 다음 화면: 직접 만듦이면 LZ-S5, 그 외에는 LZ-S3

### LZ-S5 제조 레시피

- 목적: 직접 만드는 품목의 한 판 구성을 저장한다. **원가와 재료 소모량 양쪽의 근거가 된다.**
- 입력값: 들어가는 재료와 사용량, 한 판에서 나오는 개수, 만드는 데 걸리는 시간
- 표시값: 재료를 고를 때마다 갱신되는 **한 판 재료값**과 **한 개 원가**
- 버튼: `재료 추가`, `저장`
- 기본 상태: 등록된 원부자재를 목록에서 고르고 사용량을 넣는다. 등록 안 된 재료는 여기서 바로 추가할 수 있어야 한다
- 계산 즉시 표시: 재료를 하나 넣을 때마다 원가가 바뀌는 걸 보여준다. **다 넣고 나서야 숫자를 보여주면 무엇 때문에 비싼지 알 수 없다**
- 만드는 시간을 받는 이유: 이 시간이 시급으로 환산돼 **사 오는 것과 견주는 기준**이 된다. 재료값만 보면 직접 만드는 게 싸 보이지만 시간을 넣으면 뒤집히는 경우가 많다
- 안내: 여기서 받은 시간은 **원가 비교용 참고값**이며 하루 손익에서 다시 빼지 않는다는 점을 한 줄로 적는다
- 오류 상태: 산출 개수가 0이면 저장 불가
- 다음 화면: LZ-S3

### LZ-S6 매장 설정

`LZ-13 매장 설정`과 같은 화면입니다. 상세 명세는 `로스제로_화면_명세.md`를 따릅니다.

최초 흐름에서 달라지는 점만 적습니다.

- 하단 버튼이 `저장`이 아니라 `다음`
- 값을 모르면 넘어갈 수 있게 한다. **여기서 막으면 앱을 못 쓴다.** 고정비를 비워 두면 수익 화면에 "고정비를 넣으면 실제 수익률이 나옵니다"를 표시하고 유도한다
- 인건비는 시급과 근무시간만 받는다. 고정비 목록에 인건비 항목을 두지 않는다
- 다음 화면: LZ-S7

### LZ-S7 설정 완료

- 목적: 등록이 끝났다는 사실과 오늘부터 할 일을 안내한다.
- 표시값: 등록 품목 수, 거래처 수, 직접 만드는 품목 수, 첫 추천이 나오는 시점
- 안내 문구
  - **메뉴는 미리 등록하지 않아도 된다**는 점을 알린다 — "오늘 마감 화면을 찍으면 메뉴가 저절로 잡힙니다"
  - 추천은 기록이 쌓여야 나온다는 점을 한 문장으로 표시
  - 파일럿 매장에는 첫 2주가 데이터를 모으는 기간이라고 안내
- 버튼: `오늘 시작하기`
- 다음 화면: LZ-01

---

## 6. 화면 제작 제약

- 모바일 360×800 기준.
- 이 구간은 W1에 확인한 실제 값(거래처별 납품 요일, 주문 마감 시각, 최소 주문 단위, 매입 단가)을 그대로 받는 자리입니다. **확인되지 않은 값을 기본값으로 채워 넣지 않습니다.**
- 메뉴와 메뉴 레시피는 여기서 받지 않습니다. 첫 사진 이후 LZ-03에서 받습니다.
- 회원가입·결제·다점포 전환 화면을 만들지 않습니다.
- 사용자에게 보이는 문구는 한 문장에 한 행동만 안내합니다.

## 7. 화면과 기능 명세 대응

| 화면 ID | 대응 기능 |
|---|---|
| LZ-S1 | 로그인 |
| LZ-S2 | 매장 정보 저장 |
| LZ-S3 | F-01 품목 등록 |
| LZ-S4 | F-01 품목 등록, F-11 유통기한 관리 기준값 |
| LZ-S5 | F-01 제조 레시피, F-10 원가 계산 기준값 |
| LZ-S6 | F-13 매장 설정 |
| LZ-S7 | — |
