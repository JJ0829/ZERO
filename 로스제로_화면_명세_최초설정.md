# 로스제로 화면 명세 — 최초 설정

## 1. 문서 목적

이 문서는 `로스제로_화면_명세.md`가 범위에서 제외한 **최초 1회 수행 구간**을 다룹니다. 서비스 플로우의 `앱 열기 → 품목 등록 완료? → 아니오 → A등급 품목 등록` 분기에 해당하며, 근거 자료는 `로스제로_MVP_기획서.md`의 F-01, 4장 발주 주기 설계, 10장 성공 지표입니다.

화면 ID는 매일 반복하는 루프(LZ-01~LZ-07)와 구분하기 위해 `LZ-S` 계열을 씁니다.

## 2. 핵심 사용자 흐름

`로그인 → 매장 정보 입력 → 품목 등록 → 등록 완료 → 오늘 할 일 홈(LZ-01)`

품목 등록은 최초 1회로 끝나지 않습니다. 메뉴가 바뀌거나 거래처가 바뀌면 다시 들어옵니다. **LZ-S3은 홈에서 언제든 진입할 수 있어야 합니다.**

## 3. 화면 목록

| 화면 ID | 화면 이름 | 목적 | 핵심 행동 | 다음 화면 |
|---|---|---|---|---|
| LZ-S1 | 로그인 | 매장 계정으로 접속한다 | 로그인 | LZ-S2 / LZ-01 |
| LZ-S2 | 매장 정보 입력 | 입력 알림과 발주 요일 계산의 기준값을 정한다 | 영업일·마감 시각 선택, 저장 | LZ-S3 |
| LZ-S3 | 품목 목록 | 등록된 A등급 품목을 확인하고 관리한다 | 품목 추가, 품목 선택, 등록 완료 | LZ-S4 / LZ-S5 |
| LZ-S4 | 품목 등록·수정 | 품목 하나의 발주 조건을 저장한다 | 값 입력, 저장 | LZ-S3 |
| LZ-S5 | 설정 완료 | 오늘부터 무엇을 하면 되는지 안내한다 | 오늘 입력 시작 | LZ-01 |

## 4. 화면별 상세 명세

### LZ-S1 로그인

- 목적: 매장 계정으로 접속한다.
- 입력값: 이메일, 비밀번호
- 버튼: `로그인`
- 기본 상태: 두 값이 모두 채워지기 전까지 버튼 비활성
- 오류 상태: 인증 실패 시 어느 값이 틀렸는지 특정하지 않고 한 문장으로 안내
- 다음 화면: 매장 정보가 없으면 LZ-S2, 품목이 0개면 LZ-S3, 둘 다 있으면 LZ-01
- 확인 필요: 파일럿 3곳은 직접 계정을 만들어 전달할 수 있습니다. 앱 안에서 가입을 받을지, 초대만으로 운영할지는 W5 파일럿 시작 전에 정합니다

### LZ-S2 매장 정보 입력

- 목적: 매일 입력 알림과 "오늘이 납품 요일인가" 판정의 기준이 되는 값을 받는다.
- 입력값: 매장 이름, 영업일(요일 다중 선택), 마감 시각
- 버튼: `다음`
- 기본 상태: 영업일은 월~일 전체가 선택된 상태로 시작하고, 쉬는 요일만 해제한다
- 안내: 마감 시각은 입력 알림을 보내는 기준이라는 점을 한 문장으로 표시한다
- 오류 상태: 영업일이 하나도 없으면 저장 불가
- 다음 화면: LZ-S3
- 주의: **매장 이름과 주소·연락처는 AI 요청에 담지 않습니다.** 이 값은 알림과 화면 표시에만 씁니다

### LZ-S3 품목 목록

- 목적: 관리 중인 A등급 품목을 확인하고 추가·수정한다.
- 표시값: 품목명, 거래처, 납품 요일, 최소 주문 단위, 등록 품목 수
- 버튼: `품목 추가`, 품목 행, `등록 완료`
- 기본 상태: 거래처별로 묶어서 표시한다
- 빈 상태: 품목이 0개면 A등급이 무엇인지(베이커리 완제품, 유제품, 토핑 과일 등 유통기한 1~7일 품목) 예시와 함께 설명하고 `품목 추가`만 표시. `등록 완료` 비활성
- 안내 상태: 등록 품목이 20종을 넘으면 입력 시간이 3분을 넘길 수 있다는 안내를 표시한다. 등록을 막지는 않는다
- 오류 상태: 목록을 불러오지 못하면 재시도 버튼 표시
- 다음 화면: 추가·선택 시 LZ-S4, 등록 완료 시 LZ-S5(최초) 또는 LZ-01(수정 진입인 경우)

### LZ-S4 품목 등록·수정

- 목적: 추천 계산에 필요한 품목 하나의 조건을 모두 저장한다. **여기서 빠진 값은 LZ-04에서 추천을 만들 수 없습니다.**
- 입력값

| 값 | 필수 | 쓰이는 곳 |
|---|---|---|
| 품목명 | 필수 | 전 화면 표시 |
| 단가 | 필수 | 폐기 금액 집계(LZ-07) |
| 유통기한 일수 | 필수 | 임박 판정, 소진 제안(LZ-06) |
| 거래처 | 필수 | 발주 내역 묶음(LZ-06) |
| 납품 요일 | 필수 | 오늘이 납품 요일인지 판정(LZ-04) |
| 주문 마감 시각 | 필수 | 확정 완료 안내(LZ-06) |
| 리드타임(주문→입고 일수) | 필수 | 다음 납품일까지 필요한 양 계산 |
| 최소 주문 단위 | 필수 | 제안 수량 반올림 |
| 연결 메뉴 | 선택 | 판매 입력(LZ-03) 메뉴명 매칭 |

- 버튼: `저장`, `삭제`(수정 진입 시에만)
- 기본 상태: 필수값이 하나라도 비면 저장 비활성
- 입력 보조: 거래처·납품 요일·주문 마감 시각은 이전에 등록한 거래처를 고르면 자동으로 채운다. 같은 값을 품목마다 반복 입력하게 두지 않는다
- 단위 안내: 최소 주문 단위는 낱개인지 박스인지 고르고, 박스면 한 박스에 몇 개인지 받는다
- 경고 상태: 삭제 시 해당 품목의 지난 기록도 함께 사라진다는 점을 확인받는다
- 오류 상태: 같은 이름의 품목이 이미 있으면 저장 전에 알린다
- 다음 화면: LZ-S3

### LZ-S5 설정 완료

- 목적: 등록이 끝났다는 사실과 오늘부터 할 일을 안내한다.
- 표시값: 등록 품목 수, 거래처 수, 첫 발주 제안이 나오는 시점
- 안내 문구: 추천은 판매 데이터가 쌓여야 나온다는 점을 한 문장으로 표시한다. 파일럿 매장에는 첫 2주가 데이터를 모으는 기간이라고 안내한다
- 버튼: `오늘 입력 시작`
- 다음 화면: LZ-01

## 5. 화면 제작 제약

- 모바일 360×800 기준으로 시작합니다.
- 이 구간은 W1에 확인한 실제 값(거래처별 납품 요일, 주문 마감 시각, 최소 주문 단위)을 그대로 받는 자리입니다. **확인되지 않은 값을 기본값으로 채워 넣지 않습니다.**
- 품목 등록에서 B·C등급(원두, 냉동 생지, 시럽, 컵)은 다루지 않습니다.
- 회원가입·결제·다점포 전환 화면을 만들지 않습니다.
- 사용자에게 보이는 문구는 한 문장에 한 행동만 안내합니다.
- 기본·빈·오류·경고·완료 상태 중 해당 화면에 필요한 상태를 반드시 포함합니다.

## 6. 화면과 기능 명세 대응

| 화면 ID | 대응 기능 |
|---|---|
| LZ-S1 | 로그인 |
| LZ-S2 | 매장 정보 저장 |
| LZ-S3 | F-01 A등급 품목 등록 |
| LZ-S4 | F-01 A등급 품목 등록, F-06 유통기한 관리 기준값 |
| LZ-S5 | — |
