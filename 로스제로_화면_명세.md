# 로스제로 화면 명세

## 1. 문서 목적

이 문서는 `로스제로_서비스_플로우.mermaid`의 사용자 행동과 분기 조건을 실제 화면 제작 기준으로 바꾼 결과입니다. 근거 자료는 `로스제로_MVP_기획서.md`의 7장 사용자 시나리오, 8장 기능 명세(F-01~F-10), 9장 추천 로직입니다.

이 문서가 다루는 범위는 **사장이 매일 반복하는 핵심 루프**입니다. 최초 1회만 수행하는 로그인과 A등급 품목 등록(F-01)은 `로스제로_화면_명세_최초설정.md`에서 다룹니다.

## 2. 이 명세를 관통하는 원칙

**쓰는 사람이 편하도록 만드는 것이 목표입니다.** 아래 넷은 모든 화면에 적용됩니다.

1. **매일 묻는 값은 잔량 하나뿐입니다.** 폐기·품절·입고는 해당되는 날, 해당되는 품목에만 나타납니다. 대부분 0인 칸을 매일 12개씩 보여주지 않습니다.
2. **판매량은 묻지 않고 계산합니다.** `소모량 = 어제 잔량 + 오늘 입고 − 오늘 잔량 − 폐기`
3. **어제 값을 채워 둡니다.** 사장이 하는 일은 "처음부터 쓰기"가 아니라 "달라진 것만 고치기"입니다.
4. **앱이 사장을 평가하지 않습니다.** 소요 시간, 연속 입력 일수, 추천 적중 평가, 제안 채택률을 화면에 띄우지 않습니다. 이 값들은 개발자가 볼 지표이지 사장이 볼 값이 아닙니다.

## 3. 핵심 사용자 흐름

`영업 마감 → 잔량 입력 → 추천 확인 → 근거 확인·수량 조정 → 발주 확정 → 다음 날 결과 확인`

판매 입력 단계가 없습니다. 화면 이동이 한 번 줄었습니다.

## 4. 화면 목록

| 화면 ID | 화면 이름 | 목적 | 핵심 행동 | 다음 화면 |
|---|---|---|---|---|
| LZ-01 | 오늘 할 일 홈 | 오늘 무엇을 해야 하는지 한 화면에서 판단한다 | 입력 시작 | LZ-02 |
| LZ-02 | 잔량 입력 | 품목별 남은 수량을 기록한다 | 값 수정, 저장 | LZ-03 |
| LZ-03 | 발주 추천 | 품목별 제안 수량을 한눈에 확인한다 | 품목 카드 선택, 전체 확정 | LZ-04 / LZ-05 |
| LZ-04 | 추천 상세 | 제안 수량의 계산 과정을 확인하고 수량을 조정한다 | 수량 조정, 이 품목 반영 | LZ-03 |
| LZ-05 | 확정 완료 | 확정 결과와 이후 할 일을 확인한다 | 발주 내역 복사, 홈으로 | LZ-01 |
| LZ-06 | 결과 확인 | 어제 추천과 실제 결과를 비교한다 | 기간 전환, 홈으로 | LZ-01 |

## 5. 화면별 상세 명세

### LZ-01 오늘 할 일 홈

- 목적: 마감 직후 연 사장이 오늘 할 일과 지난 결과를 한 화면에서 판단한다.
- 표시값: 오늘 날짜, 오늘 입력 여부, 미입력 누적 일수, 오늘이 납품 요일인지 여부, 어제 결과 요약
- 어제 결과 요약 규칙: **어제 버린 금액을 숫자로 표시한다.** "폐기 3건"이 아니라 "어제 4,800원어치를 버렸습니다"로 쓴다. 품절이 있었으면 함께 표시한다. 결과 확인(LZ-06)으로 들어오게 만드는 유일한 자리이며, 감으로 충분하다고 느끼는 상황(R6)에 대응하는 화면 요소다
- 표시하지 않는 것: **연속 입력 일수, 입력 소요 시간.** 하루 끊기면 그만둘 이유가 되고, 시간 목표는 개발자의 것이지 사장의 과제가 아니다
- 버튼: `오늘 입력하기`, `어제 결과 보기`
- 기본 상태: 오늘 미입력이면 `오늘 입력하기`를 주 버튼으로 표시
- 완료 상태: 오늘 입력을 마쳤으면 버튼을 `입력 수정하기`로 바꾸고 오늘의 폐기·품절 요약을 함께 표시
- 빈 상태: 첫 사용일이면 어제 결과 영역에 "내일부터 결과를 보여드립니다"를 표시
- 오류 상태: 매장 정보를 불러오지 못하면 재시도 버튼 표시. 캐시된 어제 결과를 섞어 보여주지 않는다
- 다음 화면: 입력 시작 시 LZ-02, 어제 결과 선택 시 LZ-06
- 미입력 안내: 이틀 이상 밀렸으면 밀린 일수를 알리되 재촉하는 문구를 쓰지 않는다

### LZ-02 잔량 입력

- 목적: 오늘 남은 수량을 기록한다. **이 화면 하나로 하루 입력이 끝난다.**
- 입력값
  - 매일: 품목별 잔량
  - 해당될 때만: 폐기 수량, 품절 시각, 입고 수량
- 표시값: 품목 리스트, 어제 입력값(자동 채움), 유통기한 임박 배지
- 버튼: `다 했어요`
- 기본 상태: 한 행에 **품목명과 잔량 칸 하나만** 있다. 어제 값이 채워져 있고 커서는 첫 품목에 위치한다

#### 폐기 입력 (F-03)

- 각 행 오른쪽에 `버림` 을 흐린 글씨로 둔다. 누르면 그 행에서 수량 칸이 펼쳐진다
- 값을 넣은 행은 흐린 글씨가 숫자로 바뀌어 남는다
- **매일 전 품목에 폐기 칸을 두지 않는다.** 대부분 0이고, 0을 12번 확인하는 것이 이탈의 원인이다

#### 품절 확인 (F-04)

- **잔량에 0을 넣은 품목에만** 그 행 아래로 "몇 시에 다 팔렸나요?"가 펼쳐진다
- 별도의 품절 체크박스를 두지 않는다. 잔량 0이 곧 품절 후보다
- 선택지: 시각 직접 입력 / `마감 시각` / `모르겠어요`
- `모르겠어요`를 고르면 품절 사실만 저장하고 해당 일자는 평균 계산에서 제외한다. **시각을 필수로 만들면 사장이 품절 기록 자체를 피하게 된다**

#### 입고 확인 (F-05)

- **납품일 다음 입력 때만** 목록 위에 `오늘 받은 물건` 카드가 나타난다
- 발주 확정 수량이 기본값으로 채워져 있다. 그대로면 `그대로 받았어요` 한 번으로 끝난다
- 다르면 해당 품목만 수정한다
- 이 값은 소모량 계산의 입력값이자 실제 채택률 지표의 원천이다

#### 상태

- 경고 상태: 잔량이 전날보다 늘어난 품목은 해당 행 아래에 확인 요청을 표시한다. 입고를 기록하지 않았을 가능성이 크므로 입고 확인으로 연결한다. 사장이 확인하면 그대로 저장한다
- 오류 상태: 저장 실패 시 **입력값을 절대 지우지 않는다.** 화면을 되돌리지 말고 배너만 얹고 재시도 버튼을 둔다
- 다음 화면: LZ-03
- 측정: 화면 진입부터 이탈까지 소요 시간을 기록한다 (목표 2분, 품목 20종 기준). **화면에 표시하지 않는다**

### LZ-03 발주 추천

- 목적: 오늘 시켜야 할 수량을 품목별로 확인한다.
- 표시값: 품목명, 제안 수량, 주문 단위, 거래처, 주문 마감 시각, 계산 근거 한 줄 요약
- 버튼: 품목 카드, `전체 확정`
- 기본 상태: 납품 요일이면 품목별 추천 카드 목록을 표시
- 근거 표시 규칙: 계산 근거 한 줄 요약은 **카드에 항상 펼쳐진 채로 표시한다.** 접기·더보기로 감추지 않는다. F-08의 "결과와 근거 동시 표시"는 상세 화면이 아니라 이 카드에서 충족한다
- 상세 진입은 강제하지 않는다: 사장은 목록에서 바로 전체 확정할 수 있다. 대신 **품목별로 상세를 열어봤는지 여부를 기록한다.** 근거 열람률이 시간이 지나며 떨어지는지가 H0(수량 제안이 알림보다 필요한가)의 행동 근거가 된다. 진입을 강제하면 이 신호가 사라진다
- 비납품일 상태: **오늘의 폐기·품절 요약**을 대신 표시한다. 발주 제안이 없는 날에도 입력의 대가를 만든다. 버튼은 `홈으로`
- 정확도 낮음 상태: 최근 7일 데이터가 부족한 품목은 카드 상단에 "정확도 낮음" 배지와 확보된 일수를 표시. 수량 자체는 정상 표시한다
- 파일럿 수집 기간 상태: 파일럿 1~2주차에는 추천 카드를 표시하지 않고 "데이터를 모으는 기간입니다"와 누적 입력 일수를 표시한다. 이 상태에서도 입력과 결과 확인은 정상 동작한다
- 빈 상태: 추천할 품목이 없으면 이유(재고 충분·기한 경과 제외)를 함께 표시
- 오류 상태: 계산에 실패하면 **수량 자리를 비우고** 재시도 버튼을 표시한다. 임의의 수량을 채우지 않는다. 주문 마감 시각을 알리고 평소대로 발주하라고 안내한다
- 다음 화면: 카드 선택 시 LZ-04, 전체 확정 시 LZ-05

### LZ-04 추천 상세

- 목적: 제안 수량이 어떻게 나왔는지 확인하고 사장이 최종 수량을 정한다.
- 표시값: 품목명, 제안 수량, 주문 단위 환산, 계산 과정, 유통기한 임박 여부
- 계산 과정 표시 규칙
  - 순서는 추천 로직과 같다 — 7일 평균 소모량 → 품절 보정 → 요일 보정 → 재고 차감 → 납품 주기 반영 → 최소 단위 반올림
  - **값이 바뀌지 않는 단계는 표시하지 않는다.** 0이 적힌 줄은 읽는 사람에게 아무것도 주지 않는다
  - 수량은 주문 단위로도 환산해 병기한다. 사장은 "12개"가 아니라 "2박스"로 주문한다
- 근거 표시 규칙: 화면에 보이는 숫자는 모두 계산 결과값이며, 문장은 그 숫자를 옮겨 쓴 것이다. 표시 전 근거 문장의 숫자와 계산값이 일치하는지 검사한다. 불일치하면 근거를 표시하지 않는다
- 입력값: 조정 수량
- 버튼: `이 품목 반영`, `목록으로`
- 기본 상태: 조정 수량 칸에 제안 수량이 채워져 있다
- 조정 상태: 제안 수량과 다른 값을 넣으면 차이를 개수와 주문 단위로 표시한다. 조정을 막지 않는다. **최종 결정은 사장이 한다**
- 경고 상태: 최소 주문 단위의 배수가 아닌 값을 넣으면 가까운 배수 두 개를 버튼으로 제시한다. 자동으로 고치지 않는다
- 오류 상태: 근거를 불러오지 못하면 제안 수량만 표시하고 근거 영역에 확인 필요 문구를 표시. 수량 조정과 반영은 정상 동작한다
- 다음 화면: LZ-03
- 저장 규칙: 제안 수량과 확정 수량을 **둘 다** 저장한다

### LZ-05 확정 완료

- 목적: 확정한 수량을 정리해 보여주고, 실제 발주는 앱 밖에서 이뤄진다는 점을 안내한다.
- 표시값: **거래처별** 확정 수량 목록, 주문 마감 시각
- 거래처별로 묶는 이유: 사장은 품목이 아니라 거래처 단위로 전화한다. 화면 순서를 실제 행동 순서에 맞춘다
- 버튼: `발주 내역 복사`, `홈으로`
- 안내 문구: 실제 발주는 전화·메신저·발주앱에서 진행한다는 사실을 한 문장으로 표시한다
- 소진 제안 카드 (F-10, 우선순위 S): 유통기한 임박 품목이 있으면 할인·세트 구성 등 실행 가능한 방법을 함께 표시한다. **기한이 경과한 품목은 화면에 올리기 전 코드가 제외한다.** 제안 하단에 재료 상태를 직접 확인하라는 안내를 고정 표시한다
- 빈 상태: 임박 품목이 없으면 소진 제안 카드를 표시하지 않는다. 빈 카드를 남기지 않는다. 임박 품목이 없으면 AI를 호출하지 않는다
- 다음 화면: LZ-01
- 결과 확인(LZ-06)으로 가는 버튼을 두지 않는다: 확정 직후에 볼 수 있는 것은 오늘 발주의 결과가 아니라 어제 결과이며, 이 시점의 사장은 앱을 닫으려는 상태다. 결과 확인의 진입점은 다음 날 홈(LZ-01) 하나로 유지한다

### LZ-06 결과 확인

- 목적: 추천이 맞았는지, 폐기와 품절이 줄었는지 사장이 직접 확인한다.
- 표시값
  - 어제 기준: 품목별 어제 추천 수량, 실제 소모량, 오늘 잔량, 폐기량, 품절 발생 여부
  - 주간 기준: 폐기 금액, 품절 발생률, 입력 지속 일수
- 표를 쓰는 이유: 추천·소모·남음·버림을 같은 행에서 비교해야 어디가 틀렸는지 보인다
- 품절을 별도로 강조한다: 남은 것은 눈에 보이지만 못 판 것은 보이지 않는다. 품절이 있었던 날은 표 밖에 한 번 더 적는다. 이것이 R13(폐기만 줄여 품절 증가)을 사장이 알아차리는 유일한 자리다
- 주간 화면에서 폐기 금액과 품절을 반드시 붙여 놓는다: 폐기가 줄고 품절이 늘면 실패다. 품절이 늘지 않았을 때만 성과로 말한다
- 표시하지 않는 것: **추천 적중 평가 문구, 제안 채택률.** 맞았는지는 사장이 표를 보고 판단할 몫이고, 채택률은 개발자가 볼 지표다
- 버튼: `어제 / 이번 주` 전환, `홈으로`
- 기본 상태: 어제 기준 화면이 먼저 열린다
- 빈 상태: 비교할 전날 데이터가 없으면 필요한 누적 일수를 표시하고, 오늘 기록한 내용을 대신 보여준다. 결과 확인(2일)과 추천 정확도(7일)의 기준이 다르다는 점을 함께 적는다
- 오류 상태: 집계 실패 시 **기록은 무사하고 집계만 실패했다**는 점을 알린다. 표 값은 비우고 캐시로 채우지 않는다. 오늘 입력으로 가는 길을 열어 준다
- 다음 화면: LZ-01

## 6. 화면 제작 제약

- 모바일 360×800 기준으로 시작합니다. 마감 후 한 손으로 쓰는 상황을 전제합니다.
- 화면에 나오는 모든 수량은 코드가 계산한 값입니다. **AI는 숫자를 만들지 않고 문장만 다듬습니다.**
- 흐름도에 없는 결제·자동 발주·원가 회계·다점포 관리 기능을 추가하지 않습니다.
- 로그인, 품목 등록(F-01), B·C등급 품목은 이 문서의 범위가 아닙니다.
- 사용자에게 보이는 문구는 한 문장에 한 행동만 안내합니다.
- 기본·빈·오류·경고·완료 상태 중 해당 화면에 필요한 상태를 반드시 포함합니다.
- 파일럿 1단계(1~2주차)는 별도 화면이 아니라 LZ-03의 한 상태로 구현합니다.
- 화면에 매장명·주소·연락처·결제 정보를 AI 요청에 담지 않습니다. 전달 대상은 품목명과 계산된 수치뿐입니다.

## 7. 화면과 기능 명세 대응

| 화면 ID | 대응 기능 |
|---|---|
| LZ-01 | 미입력일 감지 |
| LZ-02 | F-02 일일 잔량 입력, F-03 폐기 수량 입력, F-04 품절 기록, F-05 입고 확인, F-06 유통기한 관리 |
| LZ-03 | F-07 발주량 제안 |
| LZ-04 | F-07 발주량 제안, F-08 추천 근거 표시 |
| LZ-05 | F-10 임박 재료 소진 제안 |
| LZ-06 | F-09 결과 확인 화면 |

## 8. 이전 판(7화면)에서 달라진 점

| 항목 | 이전 | 지금 | 이유 |
|---|---|---|---|
| 화면 수 | 7개 | **6개** | 판매 입력 화면 삭제 |
| 매일 입력값 | 약 36개 | **잔량 12개 + 예외 몇 건** | 판매를 계산으로 대체, 폐기·품절을 조건부로 |
| 품절 입력 | 체크박스 12개 | **잔량 0인 품목만 질문** | 잔량 0이 곧 품절 후보 |
| 폐기 입력 | 전 품목 칸 12개 | **해당 행에서만 펼침** | 대부분 0인 칸을 매일 보여주지 않음 |
| 입고 | 계산으로 역산 | **납품일에 확인** | 소모량 계산에 필요하고, 확인값이 역산보다 정확 |
| 목표 입력 시간 | 3분 | **2분** | 입력값이 3분의 1로 줄어듦 |
