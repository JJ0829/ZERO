# 로스제로 화면 명세

## 1. 문서 목적

`로스제로_서비스_플로우.mermaid`의 사용자 행동과 분기 조건을 화면 제작 기준으로 바꾼 결과입니다. 근거 자료는 `로스제로_MVP_기획서.md`의 7장 사용자 시나리오, 8장 기능 명세(F-01~F-13), 9장 추천 로직과 9-1장 손익 계산입니다.

눌러 보는 판은 `prototype.html`, 인쇄해서 검토하는 판은 `wireframe.html`에 있습니다. 세 문서의 화면 ID는 같습니다.

## 2. 이 명세를 관통하는 원칙

1. **재고를 매일 세지 않습니다.** `오늘 재고 = 어제 재고 + 입고 − 소모 − 폐기`로 계산하고 주 1회만 확인합니다. 마감 후 냉장고를 열고 세는 일을 매일 만드는 도구는 쓰이지 않습니다.
2. **매출은 사진으로 받습니다.** 파일을 내려받아 올리는 건 매일 하기엔 귀찮습니다.
3. **매일 하는 입력은 버린 것 하나입니다.** 폐기·입고·레시피는 해당되는 날, 해당되는 품목에만 나타납니다.
4. **돈 계산은 빈틈없이 합니다.** 재료값·인건비·폐기·카드수수료·고정비·부가세를 다 넣습니다. 빠뜨린 항목이 있으면 그 숫자는 쓸 수 없습니다.
5. **인건비는 시급 하나로만 뺍니다.** 고정비에 인건비를 두고 원가에도 만드는 인건비를 넣으면 같은 돈을 두 번 뺍니다.
6. **앱이 사장을 평가하지 않습니다.** 소요 시간·연속 입력 일수·추천 적중 평가·채택률을 화면에 띄우지 않습니다. 개발자가 볼 지표이지 사장이 볼 값이 아닙니다.

## 3. 핵심 사용자 흐름

`영업 마감 → 매출 찍기 → (모르는 메뉴 있으면) 재료 알려주기 → 오늘 구운 것 → 버린 것 적기 → (납품일이면) 받은 물건 확인 → 오늘 할 일 → 근거 확인·수량 조정 → 확정 → 다음 날 결과 확인`

주 1회 `재고 맞추기`가 별도로 붙습니다.

## 4. 화면 목록

| 화면 ID | 화면 이름 | 언제 | 핵심 행동 | 다음 화면 |
|---|---|---|---|---|
| LZ-01 | 오늘 할 일 홈 | 매일 | 시작하기 | LZ-02 |
| LZ-02 | 매출 찍기 | 매일 | 포스 화면 촬영, 읽은 값 확인 | LZ-03 / LZ-04 |
| LZ-03 | 재료 알려주기 | 모르는 메뉴가 팔린 날 | 재료 고르기 | LZ-04 |
| LZ-04 | 오늘 구운 것 | 직접 만드는 품목이 있을 때 | 판 수 입력 | LZ-06 |
| LZ-05 | 원가 | 원가를 눌렀을 때 | 계산 과정 확인 | LZ-04 |
| LZ-06 | 버린 것 적기 | 매일 | 버린 수량 입력 | LZ-07 / LZ-08 |
| LZ-07 | 받은 물건 확인 | 납품일 다음 입력 | 입고 확인 | LZ-08 |
| LZ-08 | 오늘 할 일 (구울 것 + 발주) | 발주 요일 | 카드 선택, 전체 확정 | LZ-09 / LZ-10 |
| LZ-09 | 추천 상세 | 카드 선택 | 근거 확인, 수량 조정 | LZ-08 |
| LZ-10 | 확정 완료 | 확정 후 | 발주 내역 복사 | LZ-01 |
| LZ-11 | 결과 확인 | 홈에서 진입 | 어제 / 버린 것 / 수익 탭 전환 | LZ-01 / LZ-12 |
| LZ-12 | 메뉴별 뜯어보기 | 수익 탭에서 진입 | 메뉴 전환 | LZ-11 |
| LZ-13 | 매장 설정 | 최초 1회 + 값 변경 시 | 고정비·인건비·과세·카드비중 입력 | LZ-11 |
| LZ-14 | 재고 맞추기 | 주 1회 | 계산 재고 확인·수정 | LZ-01 |

로그인과 품목·메뉴 등록은 `로스제로_화면_명세_최초설정.md`에서 다룹니다.

---

## 5. 화면별 상세 명세

### LZ-01 오늘 할 일 홈

- 목적: 마감 직후 연 사장이 오늘 할 일과 지난 결과를 한 화면에서 판단한다.
- 표시값: 오늘 날짜, 오늘 입력 여부, 오늘이 납품 요일인지, 어제 결과 요약, 주 1회 재고 맞추기 안내
- 어제 결과 규칙: **버린 금액을 숫자로 쓴다.** "폐기 3건"이 아니라 "어제 4,800원어치를 버렸습니다". 금액은 폐기 수량 × 원가이며, 직접 만드는 품목은 매입가가 아니라 계산된 원가를 쓴다
- 버튼: `시작하기`, `어제 결과 보기`, `지금 확인하기`(주 1회)
- 기본 상태: 오늘 미입력이면 `시작하기`가 주 버튼
- 완료 상태: 주 버튼이 `다시 하기`로 바뀌고 **오늘 버린 금액과 오늘 남은 돈**을 함께 표시. 확정한 발주가 있으면 건수와 주문 마감 시각을 적는다
- 빈 상태: 첫 사용일이면 어제 결과 자리에 "내일부터 결과를 보여드립니다"와 무엇을 넣으면 되는지 한 줄
- 오류 상태: 매장 정보 조회 실패 시 재시도 버튼. **캐시된 값을 섞어 보여주지 않는다**
- 표시하지 않는 것: 연속 입력 일수, 입력 소요 시간
- 다음 화면: 시작하기 → LZ-02 / 어제 결과 → LZ-11 / 재고 확인 → LZ-14

### LZ-02 매출 찍기

- 목적: 메뉴별 판매 수량을 받는다. 재료 소모량·재고·원가·이익이 전부 이 값에서 계산된다.
- 입력: 포스 마감 정산 화면 사진
- 버튼: `사진 찍기`, `앨범에서 고르기`, `직접 입력하기`, `다시 찍기`
- 찍기 전 상태
  - 촬영 버튼을 화면의 절반 이상으로 크게 둔다
  - 무엇을 찍어야 하는지 한 줄로 적는다
  - **글자를 못 읽을 수 있다는 걸 찍기 전에 알린다.** 실패를 겪고 나서 알면 신뢰를 잃는다
- 읽은 뒤 상태
  - 찍은 사진 썸네일과 읽은 줄 수를 보여준다
  - **자신 없는 줄만** 배경을 달리해 표시하고 그 줄에만 +/− 버튼을 둔다. 전부 다시 확인시키면 직접 입력과 다를 게 없다
  - 왜 확인이 필요한지 쓴다 — "흐릿해요"
  - 레시피가 없는 메뉴는 "재료 모름"을 미리 표시한다
- 오류 상태: 읽지 못하면 파일명과 함께 알리고 **직접 입력을 주 버튼으로** 준다. 하루라도 기록이 비면 7일 평균이 흔들린다
- 학습: 사장이 고친 값을 저장해 다음 인식에 쓴다
- 보관: 찍은 사진을 남긴다. 숫자가 이상할 때 원본을 다시 볼 수 있어야 한다
- 다음 화면: 모르는 메뉴 있으면 LZ-03, 없으면 LZ-04

### LZ-03 재료 알려주기

- 목적: 새 메뉴의 **판매가와 재료**를 받는다. 판매가는 매출·이익의, 재료는 원부자재 소모량과 원가의 근거가 된다.
- 표시값: 메뉴명, 오늘 팔린 수량, 판매가 입력란, 재료 후보 칩, 고를 때마다 갱신되는 한 개 원가와 남는 돈
- 버튼: `나중에`, `저장하고 다음`, 재료 칩
- 메뉴는 미리 등록하지 않는다: 포스 사진에서 메뉴명이 자동으로 잡히므로 사장이 메뉴 30개를 손으로 칠 일이 없다. **이 화면이 곧 메뉴 등록이다**
- 판매가를 여기서 받는 이유
  - 포스 마감 화면에는 수량만 찍히고 단가는 없는 경우가 많다
  - 판매가가 없으면 그 메뉴는 **매출에도 이익에도 들어갈 수 없다**
  - 재료와 함께 한 번에 받으면 묻는 횟수가 늘지 않는다
  - 판매가만 넣고 재료를 넘기는 것도 허용한다. 매출은 잡히고 원가만 미상이 된다
- 기본 상태(모르는 메뉴 있음)
  - **많이 팔린 메뉴부터 하루 1~2개만** 묻는다. 시작 전에 100줄을 등록시키지 않는다
  - 등록된 품목과 흔한 사용량을 후보 칩으로 미리 만들어 둔다. 숫자를 직접 치는 건 고칠 때만
  - 재료를 하나 고를 때마다 **한 개 원가와 남는 돈이 갱신**된다. 다 넣고 나서야 숫자를 보여주면 무엇 때문에 안 남는지 알 수 없다
  - **`나중에`로 넘길 수 있어야 한다.** 강제하면 마감 시간에 걸려 다음 날 앱을 안 연다
  - 넘긴 메뉴는 소모량 미상으로 두고 억지로 추정하지 않는다
- 다 앎 상태(모르는 메뉴 없음)
  - 매일 흐름에서는 건너뛰고, 목록에서 직접 들어왔을 때만 보인다
  - **지금까지 등록된 레시피 목록**을 보여주고 고칠 수 있게 한다. 직접 만드는 품목에는 배지를 붙인다
  - "새 메뉴가 팔리면 그때 여쭤봅니다"로 이 화면이 왜 비었는지 설명한다
  - 레시피를 고치면 **그날 이후 기록에만 적용하고 지난 계산을 소급해 바꾸지 않는다**
- 판매가 미상 상태: 재료는 있는데 판매가가 없는 메뉴는 목록에 따로 모아 표시한다. 매출에 안 잡히므로 수익률이 실제보다 낮게 나온다는 점을 알린다
- 다음 화면: LZ-04

### LZ-04 오늘 구운 것

- 목적: 직접 만드는 품목을 몇 판 만들었는지 받는다. 재료 소모와 재고 증가가 여기서 계산된다.
- 표시값: 품목명, 1판 산출량, 걸리는 시간, 한 판 재료, 판 수를 넣으면 나온 개수·재료값·한 개 원가
- 입력: 판 수 (+/− 버튼)
- 버튼: `다음`, `한 조각 원가가 어떻게 나오는지 보기`
- **판 수만 받는다.** 레시피를 알고 있으니 크림치즈 500g·계란 4개를 매번 적게 하지 않는다
- 안 구운 날은 0으로 두고 넘긴다. 직접 만드는 품목이 없는 매장은 이 화면을 아예 건너뛴다
- 다음 화면: LZ-06 (원가 보기 → LZ-05)

### LZ-05 원가

- 목적: 직접 만드는 품목의 한 개당 원가가 어떻게 나왔는지 보여준다.
- 표시값
  - 한 판 재료별 사용량과 금액
  - 한 판 재료값 합계
  - 만드는 시간 × 시급
  - 산출량으로 나눈 한 개 원가
  - 같은 품목을 사 올 때 가격과의 비교
- **만드는 시간을 시급으로 환산해 보여주되 손익에서 다시 빼지 않는다.** 그 시간은 하루 인건비(시급 × 근무시간)에 이미 들어 있다. 화면에 참고값이라고 밝힌다
- 이 비교가 필요한 이유: 재료값만 보면 1,110원이라 사 오는 2,600원보다 싸 보이지만, 만드는 70분을 넣으면 2,641원이라 오히려 비싸다. 이 비교가 없으면 사장은 손해 보는 선택을 계속한다
- 같은 품목의 매입가를 모르면 비교 카드를 그리지 않는다
- 임대료·전기·수도는 여기 넣지 않는다. 메뉴 하나에 나눠 붙이지 않고 하루 매출에서 한 번에 뺀다
- 다음 화면: LZ-04

### LZ-06 버린 것 적기

- 목적: **매일 사장이 실제로 하는 유일한 입력.** 앱은 줄어든 양만 알지 팔린 건지 버린 건지 구분할 수 없다.
- 입력
  - 개수로 세는 것(사 옴 · 직접 만듦): +/− 버튼
  - 통으로 쓰는 것(원부자재): `없음 / 1/4 / 반 통 / 1통` 칩. **마감 후에 ml를 재는 사람은 없다**
- 표시값: 품목별 원가, 고른 수량의 금액, 하단에 오늘 버린 금액 합계(실시간)
- **금액을 옆에 붙인다.** "반 통"보다 "2,100원"이 다음에 덜 버리게 만든다
- 직접 만드는 품목은 계산된 원가를 쓰고 "직접 만듦"을 붙인다. 버린 케이크는 재료뿐 아니라 시간도 버린 것이다
- 버릴 게 없는 날은 전부 0인 채로 넘긴다. 확인을 강요하지 않는다
- 오류 상태: 저장 실패 시 **입력값을 절대 지우지 않는다.** 화면을 되돌리지 말고 배너만 얹고 재시도 버튼을 둔다
- 다음 화면: 납품일이면 LZ-07, 아니면 LZ-08

### LZ-07 받은 물건 확인

- 목적: 실제 입고 수량을 받는다. 재고 계산의 입력값이자 실제 채택률 지표의 원천이다.
- 표시값: 어제 확정한 거래처와 품목별 수량(기본값으로 채워짐)
- 버튼: `그대로 왔어요`, `다르게 왔어요`
- **대부분 그대로 오므로 버튼 한 번이면 끝난다.** 다를 때만 해당 품목을 고친다
- 발주한 품목만 보여준다. 전 품목에 입고 칸을 두지 않는다
- 납품일 다음 입력 때만 나타난다
- 다음 화면: LZ-08

### LZ-08 오늘 할 일 (구울 것 + 발주)

- 목적: 내일 몇 판 구울지와 오늘 무엇을 얼마나 시킬지를 한 화면에서 정한다.
- **주방과 발주를 나누지 않는다.** 굽는 카페의 진짜 고민은 "몇 개 시킬까"가 아니라 "몇 판 구울까"이고, 구울 양이 정해져야 재료 발주량이 나온다. 화면을 나누면 사장이 두 번 계산한다
- 순서: 주방이 위, 발주가 아래. 원인이 위에 있고 결과가 아래에 있다
- 표시값
  - 주방: 품목명, 구울 판 수, 나오는 개수, 계산 근거, **재료 부족 경고**
  - 발주: 거래처별 품목명, 제안 수량, 주문 단위, 주문 마감 시각, 계산 근거 한 줄
- 재료 부족 경고: 구울 양에 견줘 재료가 모자라면 그 자리에서 알리고 **발주에 자동으로 넣는다.** 사장이 스스로 환산하지 않게 한다
- 근거 표시 규칙: 계산 근거 한 줄은 **카드에 항상 펼쳐진 채로 표시한다.** 접기로 감추지 않는다. F-08의 "결과와 근거 동시 표시"는 상세 화면이 아니라 이 카드에서 충족한다
- 상세 진입을 강제하지 않는다: 목록에서 바로 전체 확정할 수 있다. 대신 **품목별로 상세를 열어봤는지 기록한다.** 근거 열람률이 H0의 행동 근거이며, 진입을 강제하면 100%로 고정되어 아무것도 알려주지 않는다
- 정확도 낮음 상태: 기록이 7일 미만인 품목은 카드에 배지와 **확보된 일수**를 표시. 수량 자체는 정상 표시한다
- 파일럿 수집 기간 상태: 파일럿 1~2주차에는 추천을 표시하지 않고 누적 일수와 시작 예정일을 보여준다. 입력과 결과 확인은 정상 동작한다
- 발주 없는 날 상태: 다음 발주일, 오늘 매출·남은 돈·수익률, 오늘 버린 내역, **내일 구울 것**을 표시. 발주가 없어도 굽는 것은 매일 할 수 있다
- 빈 상태: 추천할 품목이 없으면 이유를 두 갈래(재고 충분 / 기한 경과 제외)로 보여주고, 제외한 품목명을 알린다
- 오류 상태: **수량 자리를 비우고**(`—`) 재시도 버튼을 표시. 임의의 수량을 채우지 않는다. 주문 마감 시각을 알리고 평소대로 발주하라고 안내한다
- 다음 화면: 카드 → LZ-09 / 전체 확정 → LZ-10

### LZ-09 추천 상세

- 목적: 제안 수량이 어떻게 나왔는지 확인하고 사장이 최종 수량을 정한다.
- 표시값: 품목명, 거래처, 제안 수량, 주문 단위 환산, 계산 과정
- 계산 과정 표시 규칙
  - 추천 로직과 같은 순서로 쓴다
  - **값이 바뀌지 않는 단계는 표시하지 않는다.** 0이 적힌 줄은 읽는 사람에게 아무것도 주지 않는다
  - 계산 용어를 사장의 말로 바꾼다 — "현재 재고 차감" → "지금 남은 것 빼기", "최소 단위 반올림" → "6개 단위로 맞추기"
  - 주문 단위로도 환산해 병기한다. 사장은 "12개"가 아니라 "2박스"로 주문한다
- 근거와 계산값 일치 검사: 문장의 숫자와 계산값이 다르면 근거를 표시하지 않는다. 틀린 근거로 맞는 수량을 정당화하게 두지 않는다
- 조정 상태: 제안과 다른 값을 넣으면 차이를 개수와 주문 단위로 표시한다. **조정을 막지 않는다.** 흔한 정상 사유를 한 줄 넣어 사장이 자기 판단을 의심하지 않게 한다
- 경고 상태: 최소 주문 단위 배수가 아니면 **가까운 배수 두 개를 버튼으로** 제시하되 그대로 진행할 수 있게 둔다. 자동으로 고치지 않는다 — 사장이 넣은 값과 저장된 값이 달라지면 지표가 오염되고 신뢰를 잃는다
- 오류 상태: 근거를 불러오지 못하면 제안 수량만 표시하고 근거 영역에 확인 필요 문구. 수량 조정과 반영은 정상 동작한다
- 저장 규칙: 제안 수량과 확정 수량을 **둘 다** 저장한다
- 다음 화면: LZ-08

### LZ-10 확정 완료

- 목적: 확정한 것을 정리해 보여주고, 실제 발주는 앱 밖에서 이뤄진다는 점을 안내한다.
- 표시값: 주방(구울 판 수), 거래처별 확정 수량과 주문 마감 시각
- 순서: 주방이 맨 위 — 내일 아침 가장 먼저 할 일이다. 발주는 **거래처별로 묶는다.** 사장은 품목이 아니라 거래처 단위로 전화한다
- 버튼: `발주 내역 복사`, `홈으로`
- 안내 문구: **실제 발주는 전화·메신저로 직접 한다는 사실을 한 문장으로 표시한다.** 자동 발주로 오해하면 그날 발주가 통째로 빠지고, 실제 채택률 지표도 뜻을 잃는다
- 발주 내역 복사: 메신저에 그대로 붙여 넣을 텍스트로 복사한다. 화면을 보며 옮겨 적지 않게 하는 것이 이 버튼의 전부다
- 소진 제안 카드 (F-12, 우선순위 S)
  - 유통기한 임박 품목이 있을 때만 표시
  - **기한이 지난 품목은 화면에 올리기 전 코드가 제외한다**
  - 할인·세트·하루 메뉴 등 실행 가능한 방법으로 쓰고 금액을 함께 적는다
  - 하단에 **재료 상태는 직접 확인해 주세요**를 고정 표시한다. AI는 재료 상태를 알 수 없다
  - AI에는 품목명·수량·남은 기한만 전달한다. 상호·주소·매출은 전달하지 않는다
  - 임박 품목이 없으면 카드를 아예 그리지 않고 **AI도 호출하지 않는다**. 빈 목록으로 요청하면 없는 재료를 지어낼 여지가 생긴다
- 결과 확인으로 가는 버튼을 두지 않는다: 확정 직후에 볼 수 있는 건 어제 결과뿐이고, 이 시점의 사장은 앱을 닫으려는 상태다. LZ-11 진입점은 다음 날 홈 하나로 유지한다
- 다음 화면: LZ-01

### LZ-11 결과 확인

탭 3개로 나뉩니다.

#### 어제 탭 (기본)

- 표시값: 품목별 어제 추천 수량, 실제 나간 양, 오늘 남은 양, 버린 양, 품절 여부
- **"판매"가 아니라 "나감"으로 쓴다.** 이 값은 소모량이라 팔린 것과 시식·서비스가 섞여 있다
- 표를 쓰는 이유: 추천·나감·남음·버림을 같은 행에서 비교해야 어디가 틀렸는지 보인다
- 직접 만드는 품목은 추천이 개수가 아니라 **판 수**이므로 그대로 적고 나간 양은 개수로 적는다. 단위를 억지로 맞추지 않는다
- 품절이 있었던 날은 표 밖에 한 번 더 적는다. **남은 것은 눈에 보이지만 못 판 것은 보이지 않는다**
- 표시하지 않는 것: "어제 추천은 대체로 맞았습니다" 같은 평가 문구

#### 버린 것 탭

- 표시값: 이번 주 버린 금액, 지난주와의 차이, **매출 대비 손실률**, 품절 일수 비교, 품목별 버린 내역
- 손실률을 함께 쓰는 이유: 금액만 보면 매출이 늘어도 폐기가 늘어난 것처럼 보인다. 업계 목표는 3% 이내
- **품절을 폐기 바로 아래 둔다.** 폐기가 줄고 품절이 늘면 실패다. 품절이 늘지 않았을 때만 성과로 말하고, 늘었다면 그 사실을 먼저 쓴다
- 막대 그래프를 넣지 않는다. 금액과 차액이 이미 적혀 있고 눈금 없는 막대는 오해를 만든다

#### 수익 탭

- 표시값

```
판 것
− 재료값
− 인건비 (시급 × 하루 근무시간)
− 버린 것
− 카드 수수료 (매출 × 카드 비중 × 2.2%)
− 고정비 (하루치)
− 부가세 (일반/간이)
= 남은 것 · 수익률 · 손실률
```

- 각 줄에 **매출 대비 비율**을 함께 적는다. 금액만으로는 크고 작음을 판단할 수 없다
- 메뉴별 표: 수익률 높은 순. 인건비·고정비·부가세를 매출 비중으로, 폐기를 소모 비중으로 배부한 순이익
- **합이 맞아야 한다.** 레시피를 모르는 메뉴는 총계 매출에는 들어가고 메뉴별에서 빠지므로 그 차액을 별도 행으로 표시하고 합계 행을 둔다
- 재료 모름 안내: 금액과 함께 알리고 **수익률이 실제보다 높게 나온다**는 점을 밝힌 뒤 바로 등록하러 갈 버튼을 준다
- 표시하지 않는 것: 제안 채택률, 예측 오차
- 오류 상태: **기록은 무사하고 집계만 실패했다**는 점을 먼저 알린다. 표 값은 비우고 캐시로 채우지 않는다. 오늘 입력으로 가는 길을 열어 준다
- 빈 상태: 필요한 누적 일수를 표시하고 오늘 기록한 내용을 대신 보여준다. 결과 확인(2일)과 추천 정확도(7일) 기준이 다르다는 점을 함께 적는다
- 다음 화면: LZ-01 / 메뉴별 → LZ-12 / 설정 → LZ-13

### LZ-12 메뉴별 뜯어보기

- 목적: 메뉴 하나가 **한 개 팔릴 때 실제로 얼마 남는지**를 끝까지 보여준다.
- 표시값: 한 개 순이익(크게), 수익률, 한 개 / 전체 두 열로 판매가 − 재료값 − 인건비 몫 − 버린 것 몫 − 카드 수수료 − 고정비 몫 − 부가세 = 남는 것
- 한 개 기준을 크게 두는 이유: 사장이 판단하는 단위는 총액이 아니라 한 잔이다. "이 메뉴 하나 팔면 3,173원 남는다"가 가격을 올릴지 뺄지의 근거가 된다
- **배부 기준을 화면에 반드시 적는다** — "이 메뉴가 오늘 매출의 30%라 그만큼 붙였습니다". 안 적으면 "왜 이 메뉴에 686원이나 붙지"라는 의심이 남는다
- 메뉴 전환: 상단 칩. 화면을 나가지 않고 여러 메뉴를 견줄 수 있어야 한다
- 재료 모름 상태: 계산할 수 없다고 알리고 등록하러 갈 버튼을 준다
- 한계 명시: 배부 기준이 매출 비중이라 **만드는 데 오래 걸리는 메뉴의 인건비가 실제보다 적게 붙는다.** 제조 시간 기준 배부도 가능하나 v1은 매출 비중 하나만 쓰고 파일럿에서 사장이 납득하는지 본다
- 다음 화면: LZ-11

### LZ-13 매장 설정

- 목적: 수익률 계산에 쓸 값을 받는다. **한 번만 넣으면 매일 자동으로 반영된다.**
- 입력값

| 묶음 | 항목 |
|---|---|
| 고정비 (월) | 임대료 · 전기 · 수도 · 가스 · 통신 · 소모품·기타 |
| 인건비 | 시급 · 직원 하루 근무시간 · 사장 하루 근무시간 |
| 부가세 | 일반과세 / 간이과세 |
| 결제 | 카드 비중 |

- **고정비에 인건비 항목을 두지 않는다.** 고정비에 인건비를 두고 원가에도 만드는 인건비를 넣으면 같은 돈을 두 번 뺀다. 인건비는 시급 × 근무시간 하나로만 계산하고, 케이크 굽는 70분도 그 안에 있다
- 항목을 미리 나열하는 이유: 빈 칸에 "고정비를 입력하세요"라고 두면 무엇을 넣어야 하는지 모른다. 흔한 항목을 미리 줄로 만들어 두고 해당 없는 것은 0으로 둔다
- 사장 시간을 고르게 하는 이유: 본인 시간을 비용으로 볼지는 사람마다 다르고, 넣고 뺄 때 수익률이 **52.9% → 40.4%**로 크게 바뀐다. 어느 한쪽으로 정해 버리면 다른 쪽 사장은 이 숫자를 안 믿는다. 고르게 하되 한 번은 넣고 보라고 권한다
- 과세 유형: 일반과세는 `매출세액 − 매입세액`, 간이과세는 `매출 × 15% × 10%`. 수익률이 **52.9% ↔ 59.6%**로 달라진다. **면세 품목(우유·계란·과일)은 매입세액 공제에서 뺀다**
- 카드 비중: 현금·간편결제에는 카드 수수료가 붙지 않는다. 매출 전액에 2.2%를 붙이면 과다 계산이다
- 표시값: 한 달 고정비 합계와 영업일로 나눈 하루치, 하루 인건비
- 다음 화면: LZ-11

### LZ-14 재고 맞추기

- 목적: 계산 재고와 실제 재고의 차이를 맞춘다. **매일이 아니라 주 1회.**
- 표시값: 품목별 앱이 계산한 재고, 사장이 확인한 실제 재고, 차이 나는 품목 수와 금액
- **세는 게 아니라 확인이다.** 빈 칸을 주고 채우게 하지 않고 앱이 계산한 값을 미리 채워 둔다. 대부분 그대로 넘어가고 다른 것만 고친다
- 차이를 금액으로 보여주는 이유: 차이는 시식·증정·미기록 폐기다. 이것도 손실이다
- 레시피 점검 신호: 특정 품목에서 매주 같은 방향으로 차이가 크면 **레시피가 실제와 다르다는 뜻**이다. 그때 레시피를 다시 확인하라고 안내한다
- 주기: 매주 같은 요일. 매장이 가장 한가한 날로 고를 수 있게 한다
- 다음 화면: LZ-01

---

## 6. 화면 제작 제약

- 모바일 360×800 기준. 마감 후 한 손으로 쓰는 상황을 전제합니다.
- 화면에 나오는 모든 수량과 금액은 **코드가 계산한 값**입니다. AI는 숫자를 만들지 않고 문장만 다듬습니다.
- 흐름도에 없는 결제·자동 발주·세무 신고·다점포 관리 기능을 추가하지 않습니다.
- 사용자에게 보이는 문구는 한 문장에 한 행동만 안내합니다.
- 오류 상태에서 **수량 자리를 추정값으로 채우지 않습니다.**
- 저장 실패 시 **입력값을 지우지 않습니다.**
- 매장명·주소·연락처·매출을 AI 요청에 담지 않습니다. 전달 대상은 품목명과 계산된 수치뿐입니다.
- 파일럿 1단계(1~2주차)는 별도 화면이 아니라 LZ-08의 한 상태로 구현합니다.

## 7. 화면과 기능 명세 대응

| 화면 ID | 대응 기능 |
|---|---|
| LZ-01 | 미입력일 감지 |
| LZ-02 | F-02 POS 매출 사진 인식 |
| LZ-03 | F-03 메뉴 등록 + 레시피 |
| LZ-04 | 제조 기록, F-06 재고 자동 계산 |
| LZ-05 | F-10 원가 계산 |
| LZ-06 | F-04 폐기 입력 |
| LZ-07 | F-05 입고 확인 |
| LZ-08 | F-07 발주량·제조량 제안, F-08 추천 근거 표시, F-11 유통기한 관리 |
| LZ-09 | F-07, F-08 |
| LZ-10 | F-12 임박 재료 소진 제안 |
| LZ-11 | F-09 결과 확인, F-10 손익·수익률 집계 |
| LZ-12 | F-10 |
| LZ-13 | F-13 매장 설정 |
| LZ-14 | F-06 주간 실사 |
