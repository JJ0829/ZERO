# 로스제로 Figma 작업 지시서

## 0. 이 문서의 위치

3단계 산출물은 **수정 가능한 Figma 화면**입니다. Figma 파일은 Figma 안에서만 만들 수 있으므로, 이 문서는 그 안에서 손으로 정해야 할 값을 미리 확정해 둔 것입니다. 스타일·컴포넌트·문안을 여기서 그대로 옮기면 화면을 그리는 시간만 남습니다.

**근거 문서**

| 문서 | 이 문서가 가져온 것 |
|---|---|
| 로스제로_화면_명세.md | 화면 목적, 상태 정의, 다음 화면 |
| wireframe.html | 배치, 문안, 예시 수치 |
| 로스제로_서비스_플로우.mermaid | 화면 간 이동 |
| 로스제로_MVP_기획서.md | 무엇을 검증하는 화면인지 |

---

## 1. 만들 화면 — 5개

27개 프레임을 전부 Figma로 옮기지 않습니다. **검증에 직접 걸린 화면만** 만듭니다.

| 순서 | 화면 | 이 화면으로 확인하는 것 |
|---|---|---|
| 1 | LZ-01 홈 (기본) | 마감 후 여는 첫 화면. 폐기 금액을 보고 LZ-07로 들어오는가 (R6) |
| 2 | LZ-02 재고 입력 (기본) | 최대 이탈 구간. 20종을 3분 안에 넣을 수 있는 배치인가 (H1) |
| 3 | LZ-04 발주 추천 (기본) | 이 서비스의 존재 이유. 수량과 근거가 함께 보이는가 (H0·F-08) |
| 4 | LZ-05 추천 상세 (기본) | 계산 과정을 사장이 읽고 납득하는가 (F-08) |
| 5 | LZ-07 결과 확인 (주간) | 효과를 금액으로 체감하는가. 유료 전환의 근거 (H3) |

**뺀 화면과 이유**

- **LZ-03 판매 입력** — LZ-02와 배치가 같습니다. LZ-02 컴포넌트를 재사용하면 됩니다.
- **LZ-06 확정 완료** — 결과를 알리는 화면이라 디자인 판단이 적습니다. 4단계에서 프로토타입 연결용으로만 만듭니다.
- **오류·빈 상태 22개** — 정식 프레임으로 만들지 않고, 컴포넌트의 **변형(Variant)** 으로 정의합니다. 그래야 상태가 늘어도 화면 수가 늘지 않습니다.

---

## 2. 파일 구조

Figma 페이지를 4개로 나눕니다.

| 페이지 | 내용 |
|---|---|
| `01 스타일` | 색·글자 스타일 정의 |
| `02 컴포넌트` | 버튼·입력칸·카드 등 |
| `03 화면` | 위 5개 화면 |
| `04 프로토타입` | 4단계에서 연결선을 그릴 곳 |

프레임 이름은 화면 ID로 씁니다 — `LZ-01 홈 / 기본`. 명세·와이어프레임·개발 문서에서 같은 이름으로 부를 수 있어야 합니다.

---

## 3. 색

와이어프레임에서는 색을 쓰지 않았습니다. 이 단계에서 정합니다.

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| `ink` | `#1A1A1A` | 본문, 숫자 |
| `ink-sub` | `#6B6B6B` | 보조 설명 |
| `line` | `#D9D9D9` | 구분선, 입력칸 테두리 |
| `bg` | `#FFFFFF` | 화면 배경 |
| `bg-soft` | `#FAFAFA` | 카드 배경 |
| `accent` | `#1F6F4A` | 주 버튼, 확정, 개선된 수치 |
| `attention` | `#B3541E` | 확인 요청, 품절, 정확도 낮음 |

**빨강을 쓰지 않습니다.** 폐기와 품절은 매일 뜨는 값입니다. 빨강으로 칠하면 매일 혼나는 화면이 되고, 사장은 앱을 안 엽니다. 주의는 `attention` 한 색으로 충분하고, 그마저도 테두리와 글자에만 씁니다. 배경을 채우지 않습니다.

**색만으로 뜻을 전하지 않습니다.** 경고에는 아이콘이나 문구를 반드시 함께 둡니다. 마감 후 어두운 매장에서 보는 화면입니다.

---

## 4. 글자 스타일

기기 기본 글꼴을 씁니다. 별도 폰트를 넣지 않습니다.

| 스타일 이름 | 크기/굵기 | 행간 | 쓰는 곳 |
|---|---|---|---|
| `num-xl` | 40 / Bold | 1.1 | LZ-05 제안 수량 |
| `num-l` | 26 / Bold | 1.2 | LZ-04 카드 수량, LZ-07 금액 |
| `title` | 17 / Bold | 1.4 | 화면 첫 문장 |
| `head` | 15 / Bold | 1.4 | 카드 제목 |
| `body` | 13 / Regular | 1.6 | 본문 |
| `list` | 12 / Regular | 1.5 | 목록 항목 |
| `sub` | 11 / Regular | 1.6 | 보조 설명, 근거 문장 |
| `micro` | 10 / Regular | 1.4 | 배지, 표 머리글 |
| `btn` | 14 / Bold | 1.0 | 주 버튼 |

---

## 5. 규격

| 항목 | 값 |
|---|---|
| 프레임 | 360 × 800 |
| 상단바 높이 | 44 |
| 하단 버튼 영역 | 64 (버튼 44 + 여백) |
| 화면 좌우 여백 | **16** |
| 요소 간 간격 | 8의 배수 (4는 같은 덩어리 안에서만) |
| 최소 터치 영역 | 44 × 44 |
| 모서리 | 카드 8, 버튼 6, 입력칸 4 |

**좌우 여백이 와이어프레임(12)과 다릅니다.** Figma에서 16으로 넓힙니다. 한 손으로 쥐고 쓰는 상황에서 가장자리 오탭이 납니다. 이 변경은 wireframe.html에 다시 반영하지 않습니다 — 와이어프레임은 배치 검토용으로 역할이 끝났습니다.

---

## 6. 컴포넌트

변형(Variant)까지 함께 만듭니다. 명세의 상태 22개가 여기로 흡수됩니다.

| 컴포넌트 | 변형 | 쓰이는 화면 |
|---|---|---|
| `Button` | 주 / 보조 / 비활성 × 대 / 소 | 전체 |
| `NumberInput` | 기본 / 자동채움 / 포커스 / 경고 | LZ-02, LZ-05 |
| `ItemRow` | 기본 / 경고 / 품절펼침 | LZ-02 |
| `RecommendCard` | 기본 / 정확도낮음 / 조정됨 | LZ-04 |
| `Badge` | 기본 / 강조 / 주의 | 전체 |
| `NoticeBox` | 안내 / 확인요청 / 오류 | LZ-02, LZ-04, LZ-05 |
| `Tabs` | 2탭 (좌 선택 / 우 선택 / 우 비활성) | LZ-03, LZ-07 |
| `SummaryRow` | 기본 / 강조 | LZ-01, LZ-07 |
| `EmptyState` | — | LZ-01, LZ-04, LZ-07 |
| `BottomBar` | 버튼 1개 / 2개 | 전체 |

**`NumberInput`의 자동채움 변형을 반드시 만듭니다.** LZ-02에서 어제 값이 채워진 칸과 사장이 방금 고친 칸은 구분되어야 합니다. 이 구분이 없으면 무엇을 확인했는지 알 수 없습니다.

---

## 7. 화면별 오토레이아웃 구조

### LZ-01 홈 (기본)
```
Frame 360×800  [세로, 간격 0]
├ TopBar        [가로, 양끝, 높이 44]  "8월 30일 (일)" / Badge "테스트카페"
├ Body          [세로, 간격 16, 여백 16, 채움]
│  ├ Text title   "오늘 입력을 아직 안 하셨어요"
│  ├ Text sub     "연속 입력 6일째"
│  ├ Card         [세로, 간격 8]  Badge 강조 + 안내 2줄
│  └ Card         [세로, 간격 8]  head 금액 + sub 내역 + Button 소
└ BottomBar     [버튼 1개]  주 "오늘 입력하기"
```

### LZ-02 재고 입력 (기본)
```
Frame 360×800
├ TopBar        "← 홈" / "재고 입력" / Badge "1 / 2"
├ Body          [세로, 간격 8, 여백 16, 채움, 세로 스크롤]
│  ├ NoticeBox 안내   "어제 값이 채워져 있습니다. 달라진 것만 고치세요."
│  ├ ColumnHead       [가로]  품목 / 잔량 / 버림 / 품절
│  └ ItemList         [세로, 간격 0]  ItemRow × 12
└ BottomBar     [버튼 1개]  주 "판매 입력으로"
```
`ItemRow` 내부 — `[가로, 간격 8, 세로 가운데]` 품목명(채움) / NumberInput 52 / NumberInput 52 / Checkbox 40

### LZ-04 발주 추천 (기본)
```
Frame 360×800
├ TopBar        "발주 추천" / Badge 강조 "8월 30일"
├ Body          [세로, 간격 8, 여백 16, 채움]
│  ├ Text sub        "A베이커리 · B유통 납품 요일입니다. 주문 마감 15:00"
│  └ CardList        RecommendCard × 3
└ BottomBar     [버튼 1개]  주 "3건 전체 확정"
```
`RecommendCard` 내부 — `[세로, 간격 8]`
```
├ Head    [가로, 양끝, 아래 정렬]
│  ├ 왼쪽  [세로]  품목명 head / 거래처·단위 sub
│  └ 오른쪽 num-l 수량 + sub "개"
└ Reason  [위 점선 구분, sub, 항상 펼침]
```
**Reason을 접히는 컴포넌트로 만들지 않습니다.** F-08의 "결과와 근거 동시 표시"를 이 카드에서 충족합니다.

### LZ-05 추천 상세 (기본)
```
Frame 360×800
├ TopBar        "← 목록" / "크로플" / Badge "A베이커리"
├ Body          [세로, 간격 16, 여백 16, 채움]
│  ├ Hero        [세로, 가운데]  sub "제안 수량" / num-xl "12" / sub "6개 단위 · 2박스"
│  ├ StepTable   [세로]  StepRow × 6   (번호 / 단계명+설명 / 값)
│  └ Adjust      [가로, 간격 8]  Button 소 "−" / NumberInput 채움 / Button 소 "+"
└ BottomBar     [버튼 2개]  보조 "목록으로" / 주 "이 품목 반영"
```

### LZ-07 결과 확인 (주간)
```
Frame 360×800
├ TopBar        "← 홈" / "결과 확인" / Badge "8/25–8/31"
├ Body          [세로, 간격 12, 여백 16, 채움]
│  ├ Tabs            "어제" / "이번 주"(선택)
│  ├ Card 폐기금액    sub 제목 / num-l 금액 / sub 비교 / 막대 2줄
│  ├ Card 품절        SummaryRow × 2 + NoticeBox 안내
│  └ Card 기록        SummaryRow × 3
└ BottomBar     [버튼 1개]  보조 "홈으로"
```
폐기 금액 카드와 품절 카드를 **반드시 붙여 놓습니다.** 폐기가 줄고 품절이 늘면 실패입니다(R13). 두 숫자를 떨어뜨리면 사장은 폐기만 봅니다.

---

## 8. 문안

wireframe.html에서 그대로 가져옵니다. **Figma에서 문구를 새로 쓰지 않습니다.** 문안은 명세에서 정한 값이고, 여기서 바꾸면 개발 문서와 어긋납니다.

옮길 때 지킬 것

- 한 문장에 한 행동만 안내합니다.
- 금액은 숫자로 씁니다. "폐기 3건"이 아니라 "어제 4,800원어치를 버렸습니다".
- 사장을 탓하는 문구를 쓰지 않습니다.
- 수량 뒤에 주문 단위를 병기합니다. "12개 (2박스)".

---

## 9. 하지 않을 것

- 로고·브랜드 색·일러스트를 만들지 않습니다. 상표 조회(R7)가 끝나지 않았습니다.
- 다크 모드를 만들지 않습니다.
- 애니메이션·전환 효과를 정하지 않습니다.
- 명세에 없는 화면을 추가하지 않습니다 (R8 기능 증식).
- 27개 상태를 전부 프레임으로 만들지 않습니다. 컴포넌트 변형으로 처리합니다.

---

## 10. 이 문서 다음

4단계에서 `04 프로토타입` 페이지에 아래 이동을 연결합니다. 전체 전이표는 `dev_handoff.md`에 정리합니다.

```
LZ-01 ──오늘 입력하기──▶ LZ-02 ──판매 입력으로──▶ LZ-03
LZ-03 ──저장하고 결과 보기──▶ LZ-04
LZ-04 ──카드──▶ LZ-05 ──이 품목 반영──▶ LZ-04
LZ-04 ──전체 확정──▶ LZ-06 ──홈으로──▶ LZ-01
LZ-01 ──어제 결과 보기──▶ LZ-07 ──홈으로──▶ LZ-01
```
