# 로스제로 Figma 작업 지시서

## 0. 이 문서의 위치

Figma 파일은 Figma 안에서만 만들 수 있으므로, 이 문서는 **그 안에서 손으로 정해야 할 값을 미리 확정해 둔 것**입니다. 스타일·컴포넌트·문안을 그대로 옮기면 화면을 그리는 시간만 남습니다.

| 문서 | 이 문서가 가져온 것 |
|---|---|
| 로스제로_화면_명세.md | 화면 목적, 상태 정의, 다음 화면 |
| prototype.html | 배치, 문안, 실제 수치, 상호작용 |
| wireframe.html | 인쇄용 화면 구성 |
| 로스제로_MVP_기획서.md | 무엇을 검증하는 화면인지 |

**문안과 숫자는 prototype.html에서 그대로 가져옵니다.** Figma에서 새로 쓰지 않습니다. 여기서 바꾸면 개발 문서와 어긋납니다.

---

## 1. 만들 화면 — 5개

14개 화면을 전부 옮기지 않습니다. **검증에 직접 걸린 것만** 만듭니다.

| 순서 | 화면 | 이 화면으로 확인하는 것 |
|---|---|---|
| 1 | LZ-01 홈 | 마감 후 여는 첫 화면. 남은 돈과 버린 금액을 보고 다시 여는가 (R6) |
| 2 | LZ-02 매출 찍기 (읽은 뒤) | 사진 인식 결과를 확인하는 흐름이 3초 안에 끝나는가 (R16) |
| 3 | LZ-08 오늘 할 일 | 이 서비스의 존재 이유. 구울 양과 발주가 한 화면에서 정해지는가 (H0·F-08) |
| 4 | LZ-11 결과 확인 (수익 탭) | 수익률을 보고 계속 쓸 값어치를 느끼는가 (H3·유료 전환) |
| 5 | LZ-06 버린 것 적기 | 매일 하는 유일한 입력. 통 단위 칩이 실제로 편한가 (H1) |

**뺀 화면과 이유**

- **LZ-03 재료 알려주기 / LZ-04 오늘 구운 것 / LZ-07 받은 물건 확인** — LZ-06과 배치가 같습니다. 같은 컴포넌트를 재사용하면 됩니다.
- **LZ-05 원가 / LZ-09 추천 상세 / LZ-12 메뉴별** — 전부 `계산 과정 목록` 하나의 변형입니다.
- **LZ-10 확정 완료 / LZ-13 설정 / LZ-14 재고 맞추기** — 디자인 판단이 적습니다. 4단계 프로토타입 연결용으로만 만듭니다.
- **오류·빈 상태** — 정식 프레임으로 만들지 않고 컴포넌트 **변형(Variant)** 으로 정의합니다. 상태가 늘어도 화면 수가 늘지 않습니다.

---

## 2. 파일 구조

| 페이지 | 내용 |
|---|---|
| `01 스타일` | 색·글자 스타일 정의 |
| `02 컴포넌트` | 버튼·입력칸·카드 등 |
| `03 화면` | 위 5개 화면 |
| `04 프로토타입` | 화면 전환 연결 |

프레임 이름은 화면 ID로 씁니다 — `LZ-08 오늘 할 일 / 기본`. 명세·와이어프레임·시제품·개발 문서에서 같은 이름으로 부를 수 있어야 합니다.

---

## 3. 색

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| `ink` | `#1A1A1A` | 본문, 숫자 |
| `ink-sub` | `#6B6B6B` | 보조 설명 |
| `line` | `#DCDCDC` | 구분선, 입력칸 테두리 |
| `bg` | `#FFFFFF` | 화면 배경 |
| `bg-soft` | `#FAFAFA` | 카드 배경, 강조 행 |
| `acc` | `#1F6F4A` | 주 버튼, 확정, 남는 돈, 개선된 수치 |
| `att` | `#B3541E` | 확인 요청, 품절, 손실, 정확도 낮음 |

**빨강을 쓰지 않습니다.** 폐기와 품절은 매일 뜨는 값입니다. 빨강으로 칠하면 매일 혼나는 화면이 되고, 그 화면은 안 열게 됩니다. 주의는 `att` 한 색으로 충분하고 테두리와 글자에만 씁니다. 배경을 채우지 않습니다.

**색만으로 뜻을 전하지 않습니다.** 경고에는 문구를 반드시 함께 둡니다. 마감 후 어두운 매장에서 보는 화면입니다.

**돈의 방향은 색으로 구분합니다.** 남는 돈은 `acc`, 나가는 돈과 손실은 `att`. 수익 화면에서 이 구분이 없으면 표가 읽히지 않습니다.

---

## 4. 글자 스타일

기기 기본 글꼴을 씁니다. 별도 폰트를 넣지 않습니다.

| 스타일 이름 | 크기/굵기 | 행간 | 쓰는 곳 |
|---|---|---|---|
| `num-xl` | 44 / Bold | 1.1 | 제안 수량, 한 개 순이익 |
| `num-l` | 28 / Bold | 1.2 | 카드 수량, 오늘 남은 돈 |
| `title` | 18 / Bold | 1.4 | 화면 첫 문장 |
| `head` | 15 / Bold | 1.4 | 카드 제목 |
| `body` | 13 / Regular | 1.6 | 본문, 목록 항목 |
| `sub` | 12 / Regular | 1.6 | 보조 설명, 근거 문장 |
| `mini` | 11 / Regular | 1.5 | 배지, 표 머리글, 단위 |
| `btn` | 13 / Bold | 1.0 | 주 버튼 |

숫자는 표에서 **오른쪽 정렬**, 항목명은 왼쪽 정렬. 금액은 세 자리마다 쉼표.

---

## 5. 규격

| 항목 | 값 |
|---|---|
| 프레임 | 360 × 800 |
| 상단바 높이 | 48 |
| 하단 버튼 영역 | 60 (버튼 44 + 여백) |
| 화면 좌우 여백 | 16 |
| 요소 간 간격 | 8의 배수 (4는 같은 덩어리 안에서만) |
| 최소 터치 영역 | 44 × 44 |
| 모서리 | 카드 10, 버튼 6, 입력칸 6, 칩 14 |

+/− 버튼은 34×34로 그리되 터치 영역을 44×44로 잡습니다. 마감 후 한 손으로 누르는 버튼입니다.

---

## 6. 컴포넌트

변형까지 함께 만듭니다. 명세의 상태들이 여기로 흡수됩니다.

| 컴포넌트 | 변형 | 쓰이는 화면 |
|---|---|---|
| `Button` | 주 / 보조 / 비활성 × 대 / 소 | 전체 |
| `Stepper` | 기본 / 강조 / 비활성 | LZ-04, LZ-06, LZ-09, LZ-14 |
| `UnitChip` | 없음 / 1/4 / 반 통 / 1통 × 선택·비선택 | LZ-06 |
| `Chip` | 선택 / 비선택 | LZ-03, LZ-12, LZ-13 |
| `ItemRow` | 기본 / 금액표시 / 펼침 / 확인요청 | LZ-02, LZ-06, LZ-07, LZ-14 |
| `RecommendCard` | 발주 / 주방 / 정확도낮음 / 조정됨 / 수량없음 | LZ-08 |
| `StepRow` | 기본 / 합계 / 나눗셈 | LZ-05, LZ-09 |
| `PLRow` | 매출 / 차감 / 합계 | LZ-11, LZ-12 |
| `Badge` | 기본 / 강조(acc) / 주의(att) | 전체 |
| `NoticeBox` | 안내(acc) / 확인요청(att) / 오류(att) | 전체 |
| `Tabs` | 3탭 × 선택 위치 | LZ-11 |
| `EmptyState` | — | LZ-01, LZ-08, LZ-11 |
| `PhotoTarget` | 찍기 전 / 찍은 뒤 썸네일 | LZ-02 |
| `BottomBar` | 버튼 1개 / 2개 / 없음 | 전체 |

**꼭 변형으로 만들어야 하는 것 셋**

- `ItemRow`의 **확인요청** — LZ-02에서 흐릿하게 읽힌 줄. 배경을 `bg-soft`로 바꾸고 Stepper를 붙입니다. 이 구분이 없으면 사장이 7줄을 다 검사하게 됩니다.
- `RecommendCard`의 **수량없음** — 계산 실패 시 숫자 자리를 `—`로 비웁니다. 추정값으로 채우면 실제 돈이 나갑니다.
- `PLRow`의 **차감** — 금액 앞에 `−`를 붙이고 매출 대비 비율 열을 함께 둡니다.

---

## 7. 화면별 오토레이아웃 구조

### LZ-01 홈
```
Frame 360×800  [세로, 간격 0]
├ TopBar        [가로, 양끝, 높이 48]  "8월 30일 (일)" / Badge "테스트카페"
├ Body          [세로, 간격 10, 여백 16, 채움]
│  ├ Text title   "오늘 매출을 올려 주세요"
│  ├ Text sub     "사진 한 장 찍고, 버린 것만 적으면 끝납니다"
│  ├ Card         Badge(acc) + 안내 2줄        ← 발주 요일일 때만
│  ├ Divider
│  ├ Card         head 금액 + sub 내역 + Button 소
│  └ Card 대시    Badge "주 1회" + 재고 맞추기
└ BottomBar     [버튼 1개]  주 "시작하기"
```

### LZ-02 매출 찍기 (읽은 뒤)
```
Frame 360×800
├ TopBar        "← 홈" / "매출 찍기" / Badge "1 / 3"
├ Body          [세로, 간격 10, 여백 16, 채움, 세로 스크롤]
│  ├ PhotoRow     [가로, 간격 10]  썸네일 60×80 / (제목·부제·다시 찍기)
│  ├ NoticeBox    "1줄은 흐릿해서 자신이 없습니다"
│  └ ItemList     ItemRow × 7  (1줄만 확인요청 변형)
└ BottomBar     [버튼 1개]  주 "다음"
```

### LZ-06 버린 것 적기
```
Frame 360×800
├ TopBar        "← 구운 것" / "버린 것 적기" / Badge "3 / 3"
├ Body          [세로, 간격 0, 여백 16, 채움, 세로 스크롤]
│  ├ Text sub     "오늘 버린 것만 적어 주세요"
│  ├ ItemList     ItemRow 금액표시 × 3   (개수로 세는 것 — Stepper)
│  ├ ItemList     ItemRow 펼침 × 5       (통으로 쓰는 것 — UnitChip 4개)
│  ├ Card soft    오늘 버린 금액 합계
│  └ NoticeBox    "통 단위로 대충 고르면 됩니다"
└ BottomBar     [버튼 1개]  주 "다 했어요"
```

### LZ-08 오늘 할 일
```
Frame 360×800
├ TopBar        "오늘 할 일" / Badge(acc) "8월 30일"
├ Body          [세로, 간격 8, 여백 16, 채움, 세로 스크롤]
│  ├ Label sub          "주방"
│  ├ RecommendCard 주방  Badge + (품목·판수) + 근거 + NoticeBox 재료부족
│  ├ Divider
│  ├ Label sub          "발주 · 마감 15:00"
│  └ RecommendCard 발주 × 3
└ BottomBar     [버튼 1개]  주 "전체 확정"
```
`RecommendCard` 내부 — `[세로, 간격 10]`
```
├ Head    [가로, 양끝, 아래 정렬]
│  ├ 왼쪽  [세로]  품목명 head / 거래처·단위 mini
│  └ 오른쪽 num-l 수량 + mini 단위
└ Reason  [위 점선 구분, sub, 항상 펼침]
```
**Reason을 접히는 컴포넌트로 만들지 않습니다.** F-08의 "결과와 근거 동시 표시"를 이 카드에서 충족합니다.

### LZ-11 결과 확인 (수익 탭)
```
Frame 360×800
├ TopBar        "← 홈" / "결과 확인" / Badge "8월 30일"
├ Body          [세로, 간격 10, 여백 16, 채움, 세로 스크롤]
│  ├ Tabs             "어제" / "버린 것" / "수익"(선택)
│  ├ Card             sub 제목 / num-l 금액 / sub 수익률·손실률
│  ├ PLTable          PLRow × 8  (항목 / 금액 / 매출 대비)
│  ├ Button 소        "매장 설정 고치기"
│  ├ Divider
│  ├ Label sub        "메뉴별 — 인건비·고정비·세금까지 다 뺀 순이익"
│  ├ MenuTable        행 × 6 + 재료모름 행 + 합계 행
│  └ Button 소        "메뉴 하나씩 뜯어보기"
└ BottomBar     [버튼 1개]  보조 "홈으로"
```
메뉴별 표의 **합계 행은 반드시 넣습니다.** 레시피를 모르는 메뉴 때문에 합이 안 맞으면 사장이 바로 알아챕니다.

---

## 8. 하지 않을 것

- 로고·브랜드 색·일러스트를 만들지 않습니다. 상표 조회(R7)가 끝나지 않았습니다.
- 다크 모드를 만들지 않습니다.
- 애니메이션·전환 효과를 정하지 않습니다.
- 명세에 없는 화면을 추가하지 않습니다 (R8 기능 증식).
- 상태를 전부 프레임으로 만들지 않습니다. 컴포넌트 변형으로 처리합니다.
- 문안을 새로 쓰지 않습니다. prototype.html에서 그대로 가져옵니다.

---

## 9. 프로토타입 연결

`04 프로토타입` 페이지에서 아래를 연결합니다. 전체 전이표는 `dev_handoff.md`에 있습니다.

```
LZ-01 ──시작하기──▶ LZ-02 ──다음──▶ LZ-03 ──▶ LZ-04 ──▶ LZ-06
LZ-06 ──다 했어요──▶ LZ-07 ──그대로 왔어요──▶ LZ-08
LZ-08 ──카드──▶ LZ-09 ──이대로 반영──▶ LZ-08
LZ-08 ──전체 확정──▶ LZ-10 ──홈으로──▶ LZ-01
LZ-01 ──어제 결과 보기──▶ LZ-11 ──메뉴별──▶ LZ-12
LZ-11 ──설정──▶ LZ-13
LZ-01 ──지금 확인하기──▶ LZ-14 ──맞습니다──▶ LZ-01
```

**이미 눌러 볼 수 있는 판이 `prototype.html`에 있습니다.** Figma 프로토타입은 디자인을 얹은 뒤 같은 흐름을 재현하는 용도이며, 흐름 검증은 시제품으로 먼저 하시면 됩니다.
