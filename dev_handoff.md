# 로스제로 개발 인계 문서

이 문서 하나로 개발을 시작할 수 있게 쓴 것입니다. 화면 전환, 데이터 구조, 계산식, 검증 규칙, 예외 처리, 개발 순서가 들어 있습니다.

| 참고 | 용도 |
|---|---|
| `prototype.html` | 눌러 보는 시제품. **동작이 헷갈리면 여기가 기준입니다** |
| `wireframe.html` | 화면별 명세 주석이 붙은 인쇄용 20장 |
| `로스제로_화면_명세.md` | 화면 목적·상태·문구 규칙 |
| `로스제로_MVP_기획서.md` | 왜 이렇게 만드는지 |

---

## 1. 화면 전이표

| From | 트리거 | To | 조건 |
|---|---|---|---|
| LZ-01 홈 | 시작하기 | LZ-02 | — |
| LZ-01 | 어제 결과 보기 | LZ-11 | 일일기록 2일 이상 |
| LZ-01 | 지금 확인하기 | LZ-14 | 마지막 실사가 7일 이상 지남 |
| LZ-02 매출 찍기 | 촬영·업로드 | LZ-02 (읽은 뒤) | — |
| LZ-02 | 다음 | LZ-03 | 레시피 없는 메뉴 ≥ 1 |
| LZ-02 | 다음 | LZ-04 | 레시피 없는 메뉴 = 0, 직접 만드는 품목 ≥ 1 |
| LZ-02 | 다음 | LZ-06 | 직접 만드는 품목 = 0 |
| LZ-03 재료 알려주기 | 저장하고 다음 / 나중에 | LZ-04 | — |
| LZ-04 오늘 구운 것 | 다음 | LZ-06 | — |
| LZ-04 | 원가 보기 | LZ-05 | — |
| LZ-05 원가 | 돌아가기 | LZ-04 | — |
| LZ-06 버린 것 적기 | 다 했어요 | LZ-07 | 어제가 납품일 |
| LZ-06 | 다 했어요 | LZ-08 | 그 외 |
| LZ-07 받은 물건 확인 | 그대로 / 다르게 | LZ-08 | — |
| LZ-08 오늘 할 일 | 카드 선택 | LZ-09 | 발주 카드만. 주방 카드는 진입 없음 |
| LZ-08 | 전체 확정 | LZ-10 | — |
| LZ-08 | 홈으로 | LZ-01 | 발주 없는 날 / 파일럿 수집 기간 |
| LZ-09 추천 상세 | 이대로 반영 / 목록으로 | LZ-08 | — |
| LZ-10 확정 완료 | 홈으로 | LZ-01 | — |
| LZ-11 결과 확인 | 메뉴 하나씩 뜯어보기 | LZ-12 | 수익 탭에서만 |
| LZ-11 | 매장 설정 고치기 | LZ-13 | 수익 탭에서만 |
| LZ-12 / LZ-13 | 돌아가기 / 저장 | LZ-11 | — |
| LZ-14 재고 맞추기 | 맞습니다 | LZ-01 | — |

**뒤로가기**는 각 화면 좌상단 링크를 따릅니다. LZ-08은 뒤로가기를 두지 않습니다 — 입력을 마친 뒤라 되돌아갈 곳이 없습니다.

**LZ-10에서 LZ-11로 가는 경로를 만들지 않습니다.** 의도된 제약입니다. 확정 직후에 볼 수 있는 건 어제 결과뿐이고, 이 시점의 사장은 앱을 닫으려는 상태입니다.

---

## 2. 데이터 구조

```
Store            매장
  openDays       한 달 영업일수 (기본 26)
  taxType        'normal' | 'simple'
  cardRatio      카드 결제 비중 (0~1)
  wage           시급
  staffHours     직원 하루 근무시간
  ownerHours     사장 하루 근무시간
  countOwnerPay  사장 시간을 비용에 넣을지 (boolean)
  pilotStage     0=일반 1=수집기간 2=추천표시
  closeTime      마감 시각

FixedCost        고정비 (인건비는 여기 두지 않는다)
  name, monthlyAmount

Item             품목
  name
  kind           'bought' | 'made' | 'material'
  supplier, deliveryDays[], leadTime, minOrderUnit, orderDeadline
  buyPrice       사 옴·원부자재만. 직접 만듦은 레시피로 계산
  perUnit, unitLabel   원부자재 용량 기준 (예: 1000, 'ml')
  shelfLifeDays
  isTaxFree      면세 여부 (우유·계란·과일)
  manageOrder    발주 추천 대상인지
  countCost      원가 계산 대상인지

MakeRecipe       직접 만드는 품목의 배치 레시피
  itemId, materialItemId, amount
  yield          한 판 산출 개수
  minutes        걸리는 시간

Menu             판매 메뉴 — 미리 등록하지 않고 첫 사진에서 잡힌 뒤 LZ-03에서 채운다
  name, posLabel
  price          LZ-03에서 받는다. 없으면 매출에 잡히지 않는다

MenuRecipe       메뉴 구성
  menuId, itemId, amount

DailyRecord      일일기록
  date
  sales[]        menuId, qty          (사진에서 읽음)
  waste[]        itemId, qty          (원부자재는 0.25/0.5/1 단위)
  received[]     itemId, qty          (납품일만)
  baked[]        itemId, batches      (직접 만드는 품목만)
  photoUrl       찍은 사진

StockCount       주간 실사
  date, itemId, countedQty, calculatedQty

OrderHistory     발주 이력
  date, itemId, suggestedQty, confirmedQty, reasonViewed
```

**소모량과 재고는 저장하지 않습니다.** 일일기록에서 계산합니다. 저장하면 원본과 계산값이 어긋나고, 어긋난 순간 어느 쪽이 맞는지 알 수 없게 됩니다.

**`manageOrder`와 `countCost`를 분리합니다.** 원두는 유통기한이 길지만 원가 계산에는 반드시 들어가고 발주 추천도 켤 수 있어야 합니다.

---

## 3. 계산식

### 3-1. 소모량

```
사 옴 · 직접 만듦 :  소모량 = Σ(메뉴 판매 수량 × 레시피 사용량)
원부자재          :  소모량 = Σ(메뉴 판매 수량 × 레시피 사용량) ÷ perUnit
                            + Σ(구운 판 수 × 배치 사용량) ÷ perUnit
```

레시피가 없는 메뉴는 **미상으로 두고 추정하지 않습니다.**

### 3-2. 재고

```
오늘 재고 = 어제 재고 + 입고 + (구운 판 수 × yield) − 소모량 − 폐기
```

주간 실사가 있으면 그 날짜의 `countedQty`를 기준점으로 다시 시작합니다.

### 3-3. 원가

```
사 옴 · 원부자재 :  원가 = buyPrice
직접 만듦        :  재료원가 = Σ(재료 buyPrice × 배치량 ÷ perUnit) ÷ yield
                   참고 인건비 = wage × minutes ÷ 60 ÷ yield
```

**참고 인건비는 손익에서 다시 빼지 않습니다.** 그 시간은 하루 인건비에 이미 들어 있습니다. 원가 화면(LZ-05)에서 사 오는 것과 견주라고 보여주는 값입니다.

### 3-4. 하루 손익

```
매출        = Σ(메뉴 판매가 × 수량)                     부가세 포함 가격
재료값      = Σ(메뉴 재료원가 × 수량)                   레시피 아는 메뉴만
인건비      = wage × (staffHours + countOwnerPay ? ownerHours : 0)
폐기 손실   = Σ(폐기 수량 × 원가)
카드 수수료 = 매출 × cardRatio × 0.022
고정비      = Σ(FixedCost) ÷ openDays
부가세      일반 : 매출 ÷ 11 − 과세매입 ÷ 11           면세 품목 제외
            간이 : 매출 × 0.15 × 0.1
순이익      = 매출 − 재료값 − 인건비 − 폐기 − 카드 − 고정비 − 부가세
수익률      = 순이익 ÷ 매출
손실률      = 폐기 손실 ÷ 매출
```

### 3-5. 메뉴별 손익

```
share       = 그 메뉴 매출 ÷ 전체 매출
인건비 몫   = 하루 인건비 × share
고정비 몫   = 하루 고정비 × share
부가세 몫   = 하루 부가세 × share
카드 수수료 = 그 메뉴 매출 × cardRatio × 0.022
폐기 몫     = Σ(품목 폐기금액 × 그 메뉴의 그 품목 소모 ÷ 전체 소모)
순이익      = 매출 − 재료값 − 인건비 몫 − 폐기 몫 − 카드 − 고정비 몫 − 부가세 몫
```

**합계 검증**: `Σ(메뉴 순이익) + 재료 모르는 메뉴 몫 = 하루 순이익`. 화면에 차액 행을 별도로 표시해 합이 맞게 합니다.

### 3-6. 발주 추천

```
0. 소모량·재고 계산                      (3-1, 3-2)
1. 최근 7일 소모량 수집                   미입력일 제외 (0으로 처리하지 않음)
2. 품절 보정                             계산 재고 0이었던 날은 수요 하한으로 처리
3. 요일 보정
4. 현재 재고 차감
5. 납품 주기·리드타임 반영
6. minOrderUnit 배수로 반올림
7. 기한 경과 품목 제외
```

### 3-7. 제조량 추천

```
필요 개수 = 7일 평균 소모량 × 요일 보정 − 현재 재고
필요 판 수 = ceil(필요 개수 ÷ yield)
필요 재료 = 필요 판 수 × 배치량
재료가 모자라면 부족분을 발주 목록에 자동 추가
```

---

## 4. 검증 규칙

| # | 검사 | 처리 |
|---|---|---|
| V1 | 계산 재고 + 입고 < 입력된 재고 | 입고 누락 가능성. 입고 확인으로 유도하되 사장이 맞다고 하면 저장 |
| V2 | 소모량 < 0 | 그날 데이터를 쓰지 않고 실사 유도 |
| V3 | 근거 문장의 숫자 ≠ 계산값 | **근거를 표시하지 않는다.** 문장을 고쳐 내보내지 않는다 |
| V4 | 조정 수량 % minOrderUnit ≠ 0 | 가까운 배수 두 개 제시. **막지 않는다** |
| V5 | 유통기한 경과 품목 | AI 호출 **전에** 코드가 제외. 추천·소진 제안 모두에서 뺀다 |
| V6 | 사진 인식 신뢰도 낮음 | 해당 줄만 확인 요청. 나머지는 숫자만 표시 |
| V7 | 레시피 없는 메뉴 | 소모량 미상. 매출에는 넣되 원가에서 빼고 **화면에 금액과 함께 알린다** |
| V9 | 판매가 없는 메뉴 | **매출에 넣지 않는다.** 수익률이 실제보다 낮게 나오므로 목록에 모아 알리고 LZ-03으로 유도 |
| V8 | 임박 품목 0개 | 소진 제안 AI를 **호출하지 않는다** |

---

## 5. 예외 처리

| 상황 | 처리 |
|---|---|
| 저장 실패 | **입력값을 지우지 않는다.** 화면 유지 + 배너 + 재시도. 저장 시도 전 기기에 임시 보관 |
| 추천 계산 실패 | 수량 자리를 `—`로 비운다. **어제 값이나 평균으로 채우지 않는다.** 주문 마감 시각을 알리고 평소대로 발주하라고 안내 |
| 집계 실패 | "기록은 무사하고 집계만 실패했다"를 먼저 알린다. 표 값은 비우고 캐시로 채우지 않는다 |
| 사진 인식 실패 | 파일명과 함께 알리고 **직접 입력을 주 버튼으로** 준다 |
| 매장 정보 조회 실패 | 입력 버튼 비활성. 캐시된 어제 결과를 섞어 보여주지 않는다 |
| 데이터 7일 미만 | 추천은 표시하되 카드에 "정확도 낮음 · 기록 N일" 배지 |
| 파일럿 1~2주차 | LZ-08을 수집 기간 상태로. 입력과 결과 확인은 정상 동작 |

---

## 6. AI를 쓰는 곳과 안 쓰는 곳

| 하는 일 | 주체 |
|---|---|
| 저장, 소모량·재고 계산, 7일 평균, 품절·요일 보정, 단위 반올림, 기한 판정, 원가·손익 집계 | **코드** |
| 사진에서 메뉴명·수량 읽기, 메뉴명 매칭 후보 제시 | AI |
| 계산된 숫자를 문장으로 다듬기 | AI |
| 임박 재료 소진 방법 제안 | AI |

**숫자는 AI가 만들지 않습니다.** 코드가 계산한 값을 프롬프트에 넣고 AI는 문장만 다듬습니다.

**AI에 보내는 것**: 품목명, 계산된 수치, 남은 기한
**AI에 안 보내는 것**: 상호, 주소, 연락처, 총매출, 결제 정보, 사진 원본의 매장 식별 정보

---

## 7. 측정 항목

화면에는 띄우지 않고 저장만 합니다.

| 항목 | 산출 |
|---|---|
| 입력 지속률 | 주간 입력 일수 |
| 근거 열람률 | 추천 품목 중 LZ-09에 진입한 비율 (H0의 행동 근거) |
| 실제 채택률 | 제안 수량과 확인된 입고량의 일치율 |
| 입력 소요 시간 | LZ-02 진입 ~ LZ-06 이탈 |
| 레시피 확보율 | 레시피가 등록된 메뉴의 판매 비중 |
| 사진 인식 정확도 | 사장이 고친 줄 ÷ 전체 줄 |

**이 값들을 사장 화면에 띄우지 않습니다.** 개발자가 볼 지표이지 사장이 볼 값이 아닙니다.

---

## 8. 개발 순서

기획서 13장 10주 일정과 같습니다.

| 주차 | 만드는 것 | 끝났다고 볼 기준 |
|---|---|---|
| W3 | 품목·메뉴·레시피 등록, 사진 인식 (F-01~03) | 사진 한 장으로 메뉴별 판매가 저장된다 |
| W4 | 폐기·입고·제조 입력, 재고 계산, 주간 실사 (F-04~06) | 재고가 계산되고 실사로 맞출 수 있다 |
| W5 | 발주·제조량 추천, 근거 표시 (F-07~08) | 근거와 수량이 함께 나온다 |
| W6 | 결과 확인, 손익·수익률 집계, 매장 설정 (F-09~F-13) | **MVP 완료 기준 달성** |

**W3에서 사진 인식이 막히면 직접 입력으로 먼저 붙이고 넘어갑니다.** 인식 정확도에 W3를 다 쓰면 뒤가 전부 밀립니다.

**F-12 소진 제안은 우선순위 S입니다.** 일정이 밀리면 이것부터 뺍니다.

---

## 9. 개발 중 지킬 것

1. **범위를 넓히지 않습니다.** 기획서 6장 표에 없는 기능은 추가하지 않습니다 (R8).
2. **숫자를 추정으로 채우지 않습니다.** 모르면 비우고 모른다고 씁니다.
3. **입력값을 지우지 않습니다.** 다시 입력하게 만드는 순간 그 사장은 내일 앱을 안 엽니다.
4. **사장을 평가하지 않습니다.** 소요 시간·연속 일수·적중률을 화면에 띄우지 않습니다.
5. **인건비를 두 번 빼지 않습니다.** 시급 × 근무시간 한 번만입니다.
6. **합이 맞아야 합니다.** 메뉴별 순이익의 합과 하루 순이익이 다르면 차액을 화면에 표시합니다.
