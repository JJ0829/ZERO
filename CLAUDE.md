# 로스제로 — 작업 지침

베이커리를 파는 카페에 **내일 몇 개 시키고 몇 판 구울지**를 숫자와 근거로 알려주고,
**매일의 실제 수익률**을 보여주는 도구. 지금은 기획·설계 단계이고 앱 코드는 없다.

## 저장소 구조

기획 산출물만 있다. 빌드 도구·패키지 매니저·테스트 없음.
`prototype.html`과 `wireframe.html`은 의존성 없는 단일 HTML이라 브라우저로 바로 연다.

| 파일 | 역할 |
|---|---|
| `로스제로_MVP_기획서.md` | 문제·가설·범위·리스크. **범위의 기준은 6장 표** |
| `dev_handoff.md` | 전이표·데이터 구조·계산식·검증 규칙 (9장이 개발 원칙) |
| `로스제로_화면_명세.md` / `_최초설정.md` | 화면별 목적·상태·문구·전환 |
| `prototype.html` | 클릭되는 시제품. 화면 정의는 `V` 객체, 내비는 `NAV` 배열 |
| `wireframe.html` | 와이어프레임 20장 (A4 인쇄용) |
| `figma_guide.md` | Figma 작업 지시서 |

## 반드시 지킬 것

`dev_handoff.md` 9장이 원본이고, 요약하면:

1. **범위를 넓히지 않는다** — 기획서 6장 표에 없는 기능은 추가하지 않는다
2. **숫자를 추정으로 채우지 않는다** — 모르면 비우고 모른다고 쓴다
3. **입력값을 지우지 않는다** — 다시 입력시키는 순간 그 사장은 내일 앱을 안 연다
4. **사장을 평가하지 않는다** — 소요 시간·연속 일수·적중률을 화면에 띄우지 않는다
5. **인건비를 두 번 빼지 않는다** — 시급 × 근무시간, 한 번만
6. **합이 맞아야 한다** — 메뉴별 순이익 합 ≠ 하루 순이익이면 차액을 화면에 표시

## 손익 계산

```
매출 − 재료값 − 인건비 − 폐기손실 − 카드수수료(매출×카드비중×2.2%)
     − 고정비(월합÷영업일수) − 부가세 = 순이익
수익률 = 순이익 ÷ 매출     손실률 = 폐기손실 ÷ 매출
```

인건비는 시급 하나로만 뺀다. 케이크 굽는 70분도 근무시간 안이므로 원가에 또 더하지 않는다.
고정비 항목에 인건비를 두면 같은 돈을 두 번 빼게 된다.

## 화면 문구

사장이 새벽에 한 손으로 보는 화면이다. **결론 한 줄 + 근거 한 줄**로 쓴다.

```
내일 크로플 12개 시키세요
  7일 평균 13개 · 주말이라 +3개 · 지금 남은 것 2개 · 6개 단위로 맞춤
```

근거 없는 숫자는 내보내지 않는다.

## 품목 세 종류

| 구분 | 예 | 원가 | 발주 대상 |
|---|---|---|---|
| 사 옴 | 크로플, 초코쿠키 | 매입가 | 그 품목 |
| 직접 만듦 | 바스크치즈케이크 | 재료값 ÷ 나오는 개수 | 재료 |
| 원부자재 | 우유, 생크림, 크림치즈, 원두 | 매입가 | 그 품목 |

발주 관리와 원가 계산은 따로 켠다. 원두처럼 유통기한이 긴 재료도 원가에는 반드시 들어간다.

## 노트북 2대 작업

노트북 두 대에서 번갈아 작업한다. **OneDrive는 쓰지 않는다** — 이 폴더는 OneDrive 밖에 있고,
`.git`을 OneDrive에 넣으면 저장소가 깨진다. 이동은 GitHub(`JJ0829/ZERO`)으로만 한다.

- `wip` — 작업 브랜치. 노트북 옮길 때 짐 싣는 용도라 커밋이 잦아도 된다
- `main` — 정리된 상태만. `wip`이 마무리되면 한 번에 합친다

세션 시작 시 `git pull`은 훅으로 자동 실행된다. **노트북 덮기 전에는 push를 해야 한다.**

## 지금 상태

기획·화면 설계 완료. 개발 전 검증이 남았다.

- [ ] 지인 카페 POS 매출 파일 실물 확인 — **안 되면 설계 전체가 성립하지 않는다**
- [ ] 키프리스 "로스제로" 상표 조회
- [ ] 비지인 카페 10곳 인터뷰 (H0 · H2)
- [ ] 개발 착수
